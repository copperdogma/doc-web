# Cursor Rules

Use this directory only for Cursor-specific activation behavior that cannot be expressed in `AGENTS.md` or subdirectory `AGENTS.md` files.

Do not duplicate core project instructions here.

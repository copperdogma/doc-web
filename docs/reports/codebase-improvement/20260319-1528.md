# Codebase Improvement Scan — 20260319-1528

**Scanned:** 2026-03-19 15:28
**Branch:** detached HEAD
**HEAD:** 0ee5bbd
**Scope:** mid-story repo mission-alignment audit during Story 155
**Mode:** report-only at scan time
**Status:** historical scan; resolved by Story 155 closeout

## Summary

- Auto-fix candidates: 0 open
- Story candidates: 0 open
- Suppressed: 0
- Ignored: 1
- Recommended next step: none for Story 155. Use `docs/notes/repo-mission-alignment-cleanup-inventory.md` for the final cleanup state.

## Historical Note

This scan captured the repo before the final Story 155 removal tranche. It is
kept as evidence of what still looked live mid-cleanup, not as current
guidance. The FF runtime, sample-book residue, and `tools/run_manager.py` FF
default called out below were removed or rewritten before closeout.

## Detectors Run

- `git status --short` — dirty worktree from Story 155; stayed report-only.
- `make check-size` — highlighted large files; several of the biggest remaining hotspots are FF export/validator modules.
- `git log --since='90 days' --name-only --format='' | sed '/^$/d' | sort | uniq -c | sort -rn | head -50` — recent churn still concentrated in FF recipes and builder/validator stack.
- `rg -n "TODO|FIXME|XXX|HACK|TEMP|placeholder|stub" ...` — signal dominated by intentional stub/validator vendored code; not the main cleanup vector.
- Targeted `rg` across `configs/recipes`, `modules`, `tests`, `docs`, `scripts`, and `tools` — identified live references, stale comments, and orphaned FF prompt/doc artifacts.

## Resolved Findings

### 1. Fighting Fantasy game-ready export stack no longer matches the mission, but still has live wiring

- Classification at scan time: Story
- Status: Resolved in Story 155
- Why it mattered: This was the largest remaining non-mission surface in the repo. The active mission is intake R&D plus a versioned `doc-web` runtime boundary, but this stack still packaged `gamebook.json`, `validator/`, and engine-ready output for a legacy FF/game runtime.
- Outcome: [`tools/run_manager.py`](/Users/cam/.codex/worktrees/7c6b/doc-web/tools/run_manager.py) now seeds the active `configs/recipes/recipe-images-ocr-html-mvp.yaml` path, and the FF builder/validator/package cluster plus dependent top-level recipes were removed. The final classification lives in [`docs/notes/repo-mission-alignment-cleanup-inventory.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/docs/notes/repo-mission-alignment-cleanup-inventory.md).

### 2. A smaller set of root-level FF fossils appear to be removable now

- Classification at scan time: Auto-fix
- Status: Resolved in Story 155
- Why it mattered: These files added noise, misled readers about the current mission, and did not support active runtime paths.
- Outcome: the root fossils called out here were removed as part of the coordinated Story 155 cleanup and no longer appear in the active repo surface.

### 3. FF-specific regression and OCR benchmark helpers are preserved as if they are current tooling

- Classification at scan time: Story
- Status: Resolved in Story 155
- Why it mattered: This cluster was not part of the current intake/`doc-web` workflow, but it still consumed maintenance attention and created a false impression that `ff-canonical` remained a supported benchmark baseline.
- Outcome: the legacy FF regression and OCR benchmark helpers, tests, and fixture packs identified here were removed with the retired runtime.

### 4. Mission documentation still carries stale pre-doc-web pipeline guidance

- Classification at scan time: Auto-fix
- Status: Resolved in Story 155
- Why it mattered: These docs were still presented as current project guidance, but they described the old book/gamebook pipeline rather than the accepted `doc-web` runtime boundary.
- Outcome: [`docs/requirements.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/docs/requirements.md), [`docs/document-ir.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/docs/document-ir.md), [`docs/stories.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/docs/stories.md), [`README.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/README.md), [`AGENTS.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/AGENTS.md), and [`docs/RUNBOOK.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/docs/RUNBOOK.md) now point at intake + `doc-web` and stop presenting FF/sample-book work as active guidance.

## Suppressions / Ignores

- Large generic modules such as OCR and intake hotspots from `make check-size` — important, but not evidence of mission drift by themselves.

## Verification / Notes

- Historical record only. For the final classification, see [`docs/notes/repo-mission-alignment-cleanup-inventory.md`](/Users/cam/.codex/worktrees/7c6b/doc-web/docs/notes/repo-mission-alignment-cleanup-inventory.md) and Story 155.
- Worktree state at scan time: dirty
- Checks run at scan time: `git status --short`, `make check-size`, recent hotspot log, repo-wide `rg` on FF/gamebook/default-path terms, targeted file inspection for `tools/run_manager.py`, legacy prompts, legacy scripts, requirements/docs drift, packaging/export modules, and FF benchmark helpers.

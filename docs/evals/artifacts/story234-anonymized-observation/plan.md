# Story234 anonymized structural observation — frozen plan

Six fictional samples inherit the entire 48-row table structure of one real,
reviewed chapter023. All cell text and non-structural attributes were regenerated;
no original names, dates, quotations or personal content are sent. Case1 is the
unmutated **generated counterpart**, not original source content. Cases2–6 have
explicit injected mutations. This is a substantial maintained-layout sample,
not six independent documents, production traffic or source-fidelity evidence.

Use the unchanged maintained dossier builder/detector and full GPT4.1 planner
prompt/JSON contract once, then actual runtime native JEV1.13.0 shadow in two
three-chapter batches. Fresh planner conventions are retained unchanged, even if
poor or inconsistent. Gold is separate and never passed to either provider.
Explicit uncertainty, .8 confidence policy, layout guard and two-second native
caller deadline remain frozen. No production parser changes or retries.

## Pre-call adjudication

Root reviewed generated HTML/dossier/gold: cases1–5 supported. Case6 intentionally
contains an ambiguous event in full HTML, but maintained compaction drops it.
**Exclude case6 from conditional classifier accuracy.** Report it separately as
an end-to-end compaction/coverage probe, including absent page-context detection;
do not attribute unseen text to model error. Report six-case pipeline behavior
separately from five-case conditional classification. No post-output input fixes.

Compare raw JEV, full planner status, binary unchanged detector within its coverage,
and actual shadow routes. Report direct JEV coverage, abstentions, fallbacks,
false-clean counts and source-reviewed misses. Missing conventions/input bounds
are coverage gaps, not paid retries. Baseline full-planner cost PLUS JEV cost is
added overhead, never claim savings. One cohort/call is descriptive only.

## Calls and custody

One full planner, at most six JEV calls; maximum total $0.15. Planner reservation
uses serialized Unicode bytes +2048 overhead, $2/M input, 6000 output cap at$8/M.
Each JEV reserves64k inputtokens at$.042/M=$.002688. Initial ceiling$.108506.
SDK retries0; no model retry. Distinct request/raw-response files, duplicate/run
refusal, pre-call ledger reservations, and source/fixture/prompt SHA256 freeze.
Unknown responses retain reservation. Native responses are saved before strict
Choice parsing. Native timeout may leave a daemon response unreceived; this is
unknown delivery/billing, not zero usage. No primary changes or runtime enabling.

Readiness: runtime key in ignored worktree env; existing owner OpenAI key can be
mapped only into child environment using nested owner wrappers. No secret copied.

Fairness: this is matched source cohort but different production inputs and roles.
The full planner sees detector hints and authors conventions; JEV receives the
sanitized compact profile plus those planner-authored conventions. Results measure
workflow-relative utility, not controlled model superiority. Missing original text
or compacted-away cues are input limitations, not unexplained reasoning failures.

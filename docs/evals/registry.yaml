# Doc-Forge Eval Registry
# Central source of truth for all evals, their scores, and improvement attempts.
# Updated by AI agents whenever evals are run. See AGENTS.md for protocol.
#
# Types:
#   quality     — "How good is our output?" Continuous improvement target.
#   compromise  — "Can we eliminate this compromise?" Binary pass/fail gate.
#
# Triage classes (derived, not stored as a separate field):
#   quality-runtime        — quality evals where latency/cost are active decision inputs
#   quality-capability     — quality evals focused on output fidelity / behavior quality
#   compromise-detection   — compromise evals that gate deletion of spec workarounds
# The triage skills should derive these from `type`, target fields, and context
# rather than adding another registry field unless the taxonomy needs to diverge.
#
# Score fields:
#   latency_ms      — avg per-call latency (REQUIRED when measurable)
#   cost_usd        — avg per-call cost in USD (REQUIRED if model has pricing)
#   cost_estimated  — present+true when cost was computed from tokens, not promptfoo
#
# Explicit lineage fields:
#   story_refs       — canonical story linkage for the eval
#   category_refs    — explicit category ownership when the eval spans categories
#   compromise_refs  — explicit compromise linkage
#   spec_refs        — optional direct `spec:N` / `spec:N.N` refs when needed
#
# retry_when conditions:
#   new-worker-model     — A smarter orchestrating AI might succeed
#   new-subject-model    — A better pipeline model might pass without code changes
#   cheaper-subject-model — Works on expensive model, need cost parity at lower tier
#   faster-subject-model — Works on slow model, need latency parity at faster tier
#   new-approach         — Current approaches exhausted, needs fresh thinking
#   golden-fix           — Golden reference may be wrong or incomplete
#   architecture-change  — Upstream pipeline needs to change first

evals:

  # ── Quality Evals (promptfoo) ─────────────────────────────────────────────

  - id: image-crop-extraction
    name: Image Crop Extraction
    type: quality
    description: >
      Detect and extract illustration bounding boxes from scanned book pages.
      Tests detector accuracy (IoU), count matching, and text exclusion.
    pipeline_stage: crop_illustrations
    runner: promptfoo
    command: "cd benchmarks && promptfoo eval -c tasks/image-crop-extraction.yaml --no-cache -j 3"
    config: benchmarks/tasks/image-crop-extraction.yaml
    scorer: benchmarks/scorers/image_crop_scorer.py
    golden: benchmarks/golden/
    story_refs: ["133", "183", "207"]
    category_refs: ["spec:4", "spec:8"]
    compromise_refs: ["C4"]
    spec_compromise: C4

    target:
      metric: overall
      value: 0.95
      pass_rate: 0.90

    scores:
      - model: "Gemini 3 Flash (conservative-count prompt, maintained rerun)"
        metrics:
          overall: 0.9703
          pass_rate: 1.0
        measured: 2026-04-10
        git_sha: d074136
        note: >
          Story 207 reran the maintained detector surface via
          `promptfoo eval -c tasks/image-crop-extraction.yaml --no-cache
          --filter-providers 'google:gemini-3-flash-preview'
          --filter-prompts 'conservative-count' --output
          results/story207-image-crop-baseline.json -j 1`.
          The JSON output recorded `13/13` scorer passes with `0` failures and
          `0` errors; recomputing the mean from the per-case scores yielded
          `0.9703 / 1.0` (overall / pass_rate), average latency about `7878 ms`
          per page, and total eval cost about `$0.059`. The portable tracked
          proof anchor is now
          `docs/evals/attempts/001-image-crop-extraction-story207-proof-refresh.md`;
          the raw promptfoo JSON remains a local regenerable artifact.
      - model: "Gemini 3.6 Flash (conservative-count prompt)"
        metrics: {overall: 0.9692, pass_rate: 1.0, cases_total: 13, latency_ms: 4460}
        cost_usd: 0.0897
        cost_estimated: true
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Exact-ID GA challenger completed `13/13` without provider errors but
          did not beat the maintained Gemini 3 Flash `0.9703` score and cost
          more at current standard prices. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "Gemini 3.5 Flash-Lite (conservative-count prompt)"
        metrics: {overall: 0.6427, pass_rate: 0.6154, cases_total: 13, latency_ms: 1431}
        cost_usd: 0.0083
        cost_estimated: true
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Provisional prompt-only JSON run completed `8/13`. The nested,
          scale-incompatible, and malformed outputs were later reclassified as
          pipeline/configuration-wrong after the model's structured-output API
          support was confirmed and rerun below. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "Gemini 3.5 Flash-Lite (structured output, integer 0-1000 bboxes)"
        metrics: {overall: 0.8988, pass_rate: 0.9231, cases_total: 13, latency_ms: 1591}
        cost_usd: 0.0087
        cost_estimated: true
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          API-enforced JSON Schema eliminated all malformed and nested outputs.
          A final integer-coordinate run completed `12/13` with `0` provider
          errors. Manual inspection confirmed the remaining `Image121` miss is
          model-wrong: the lower-left oval portrait was reduced to a narrow
          strip. The run clears the `0.90` pass-rate component but misses the
          `0.95` overall target and maintained `13/13`, `0.9703` detector.
          Portable proof: `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "GPT-5.5 (conservative-count prompt)"
        metrics:
          overall: 0.8111
          pass_rate: 0.7692
          cases_total: 13
          latency_ms: 9180
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh new-subject-model challenger rerun after confirming `gpt-5.5`
          exists in the local OpenAI model list. Command:
          `cd benchmarks && promptfoo eval -c tasks/image-crop-extraction.yaml
          --no-cache --filter-providers 'openai:chat:gpt-5.5'
          --filter-prompts 'conservative-count' --output
          results/gpt55-image-crop-extraction-20260424.json -j 1`.
          The run completed `10/13` with `3` scorer failures, `0` errors,
          `overall = 0.8111`, `pass_rate = 0.7692`, average latency about
          `9180 ms`, and total tokens `52692`. Estimated current GPT-5.5 API
          cost from token counts is about `$0.4080` total (`$0.0314` per case).
          Failures were bbox precision misses on `Image001`, `Image013`, and
          `Image020`, so this does not replace the maintained Gemini 3 Flash
          detector. Portable tracked proof:
          `docs/evals/attempts/003-gpt55-crop-eval-refresh.md`.
      - model: "GPT-5.5 Responses (conservative-count prompt)"
        metrics:
          overall: 0.791
          pass_rate: 0.7692
          cases_total: 13
          latency_ms: 4071
        latency_ms: 4071
        cost_usd: 0.0217
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh direct Responses API rerun after adding
          `openai:responses:gpt-5.5` and Responses-compatible image prompt
          formatting. Command:
          `cd benchmarks && promptfoo eval -c tasks/image-crop-extraction.yaml
          --no-cache --filter-providers '^openai:responses:gpt-5\.5$'
          --filter-prompts 'conservative-count' --output
          results/gpt55-responses-image-crop-extraction-gpt55-20260424.json
          -j 1`. The run completed `10/13` with `0` provider errors,
          `overall = 0.791`, `pass_rate = 0.7692`, average latency about
          `4071 ms`, and estimated current API cost about `$0.2820` total
          (`$0.0217` per case). It confirms the Chat interface did not hide a
          passing GPT-5.5 detector; the failure set shifted to `Image001`,
          `Image021`, and `Image059`. Portable tracked proof:
          `docs/evals/attempts/006-gpt55-responses-eval-refresh.md`.
      - model: "OpenAI chat-latest Responses alias (GPT-5.5 Instant, conservative-count prompt)"
        metrics:
          overall: 0.8006
          pass_rate: 0.8462
          cases_total: 13
          latency_ms: 3610
        latency_ms: 3610
        cost_usd: 0.0107
        cost_estimated: true
        measured: 2026-05-06
        git_sha: 60942e2
        note: >
          Bounded GPT-5.5 Instant alias challenger after OpenAI's 2026-05-05
          release exposed the API alias as `chat-latest`. Promptfoo 0.121.1
          sent unsupported parameters through the built-in OpenAI providers, so
          this run used the narrow
          `benchmarks/providers/openai_chat_latest_responses.py` Responses shim
          against the maintained detector task with only the provider
          overridden. Equivalent command:
          `cd benchmarks && DOC_WEB_OPENAI_API_KEY="$OPENAI_API_KEY"
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/image-crop-extraction.yaml --providers
          "python:$(pwd)/providers/openai_chat_latest_responses.py" --no-cache
          --filter-prompts 'conservative-count' --output
          results/chat-latest-image-crop-extraction-20260506.json -j 1`. The
          run completed `11/13` with `0` provider errors, `overall = 0.8006`,
          `pass_rate = 0.8462`, average latency about `3610 ms`, and estimated
          GPT-5.5-rate cost about `$0.1389` total (`$0.0107` per case).
          Failures were bbox undercoverage on `Image013` and `Image021`, so the
          alias does not replace the maintained Gemini 3 Flash detector.
          Portable tracked proof:
          `docs/evals/attempts/010-chat-latest-instant-bounded-challenger.md`.
      - model: "Kimi K2.6 (conservative-count prompt, non-thinking)"
        metrics:
          overall: 0.8981
          pass_rate: 0.9231
          cases_total: 13
          latency_ms: 6176
        latency_ms: 6176
        cost_usd: 0.0040
        cost_estimated: true
        measured: 2026-05-20
        git_sha: 37ef4af
        note: >
          Bounded Moonshot direct-API challenger after adding
          `benchmarks/providers/moonshot_kimi_chat.py` and mapping
          `DOC_WEB_MOONSHOT_API_KEY` to `MOONSHOT_API_KEY` for child promptfoo
          runs. Command: `cd benchmarks &&
          DOC_WEB_ENV_FILE=/Users/cam/Documents/Projects/doc-web/.env
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/image-crop-extraction.yaml --providers
          "python:$(pwd)/providers/moonshot_kimi_chat.py" --filter-prompts
          'conservative-count' --output
          results/kimi-k26-image-crop-extraction-20260520.json --no-cache
          -j 1`. The run completed `12/13` with `0` provider errors,
          `overall = 0.8981`, `pass_rate = 0.9231`, average latency about
          `6176 ms`, and estimated Kimi K2.6 cost about `$0.0519` total
          (`$0.0040` per case). The single miss was model-wrong on `Image001`:
          Kimi returned no images for the stylized title page that the
          maintained prompt/golden intentionally count as standalone artwork.
          A thinking-mode retry of the failed case also returned no images, so
          no full thinking-mode rerun was warranted. Portable tracked proof:
          `docs/evals/attempts/011-kimi-k26-bounded-challenger.md`.
      - model: "Gemini 3.5 Flash (conservative-count prompt)"
        metrics:
          overall: 0.9679
          pass_rate: 1.0
          cases_total: 13
          latency_ms: 9675
        latency_ms: 9675
        cost_usd: 0.0084
        cost_estimated: true
        measured: 2026-05-20
        git_sha: 99450a3
        note: >
          Bounded Gemini 3.5 Flash challenger after Conductor Scout 035 and the
          2026-05-19 Google model docs exposed stable `gemini-3.5-flash`.
          Command: `cd benchmarks && promptfoo eval -c
          tasks/image-crop-extraction.yaml --providers google:gemini-3.5-flash
          --filter-prompts 'conservative-count' --no-cache --output
          results/gemini35-flash-image-crop-extraction-20260520.json -j 1`.
          The run completed `13/13` with `0` failures and `0` provider errors,
          `overall = 0.9679`, `pass_rate = 1.0`, average latency about
          `9675 ms`, and `26609` total tokens. Estimated paid standard Gemini
          3.5 Flash cost from current Google pricing is about `$0.1095` total
          (`$0.0084` per case), while the actual run used free-tier quota. This
          passes the detector target but does not replace the maintained Gemini
          3 Flash detector because it is slightly below the current maintained
          score (`0.9703`), slower in this run, and costlier on paid standard
          pricing. Portable tracked proof:
          `docs/evals/attempts/011-gemini35-flash-bounded-challenger.md`.
      - model: "Claude Opus 4.8 (conservative-count prompt, adaptive high effort)"
        metrics:
          overall: 0.7669
          pass_rate: 0.7692
          cases_total: 13
          latency_ms: 4291
        latency_ms: 4291
        cost_usd: 0.0247
        cost_estimated: true
        measured: 2026-05-28
        git_sha: 5254df1
        note: >
          Bounded Anthropic challenger after Conductor Scout 043 and the
          2026-05-28 Anthropic Opus 4.8 release. Promptfoo 0.121.1's built-in
          Anthropic provider sent `temperature`, which Opus 4.8 rejects, so the
          run used the narrow `benchmarks/providers/anthropic_opus48_messages.py`
          provider with sampling params omitted, adaptive thinking enabled, and
          `output_config.effort = high`. Command: `cd benchmarks &&
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/image-crop-extraction.yaml --providers
          python:$(pwd)/providers/anthropic_opus48_messages.py
          --filter-prompts 'conservative-count' --output
          results/opus48-image-crop-extraction-20260528.json --no-cache -j 1`.
          The run completed `10/13` with `0` provider errors,
          `overall = 0.7669`, `pass_rate = 0.7692`, average latency about
          `4291 ms`, and estimated Opus 4.8 cost about `$0.3215` total
          (`$0.0247` per case). Failures were bbox misses on `Image021`,
          `Image037`, and `Image059`; an `xhigh` failed-case retry repaired
          `0/3`. This does not replace the maintained Gemini 3 Flash detector.
          Portable tracked proof:
          `docs/evals/attempts/012-opus48-bounded-challenger.md`.
      - model: "Claude Sonnet 5 (conservative-count prompt, adaptive high effort)"
        metrics:
          overall: 0.5334
          pass_rate: 0.3846
          cases_total: 13
          latency_ms: 3834
        latency_ms: 3834
        cost_usd: 0.0098
        cost_estimated: true
        measured: 2026-06-30
        git_sha: fb2d00b
        note: >
          Bounded Anthropic challenger after the 2026-06-29 Claude Sonnet 5 API
          release. `python scripts/discover-models.py --check-new` confirmed
          `claude-sonnet-5` is live through the repo-local Anthropic key, so the
          run reused the direct Anthropic/no-sampling provider with adaptive
          thinking enabled, `output_config` effort high, and Sonnet-specific
          introductory token prices supplied by environment variables. Command:
          `cd benchmarks && ANTHROPIC_OPUS48_MODEL=claude-sonnet-5
          ANTHROPIC_MESSAGES_INPUT_PRICE_PER_1M=2
          ANTHROPIC_MESSAGES_OUTPUT_PRICE_PER_1M=10
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/image-crop-extraction.yaml --providers
          python:$(pwd)/providers/anthropic_opus48_messages.py
          --filter-prompts 'conservative-count' --output
          results/sonnet5-image-crop-extraction-20260630.json --no-cache -j
          1`. The run completed `5/13` with `0` provider errors,
          `overall = 0.5334`, `pass_rate = 0.3846`, average latency about
          `3834 ms`, `56874` prompt tokens, `1416` completion tokens, and
          estimated Sonnet 5 cost about `$0.1279` total (`$0.0098` per case).
          Failures were model-wrong bbox/coordinate misses on `Image001`,
          `Image003`, `Image011`, `Image013`, `Image021`, `Image037`,
          `Image124`, and `Image126`. This does not replace the maintained
          Gemini 3 Flash detector. Portable tracked proof:
          `docs/evals/attempts/014-sonnet5-bounded-challenger.md`.
      - model: "GPT-5.6 Sol Responses (conservative-count prompt, reasoning none)"
        metrics:
          overall: 0.9403
          pass_rate: 1.0
          cases_total: 13
          latency_ms: 3223
        latency_ms: 3223
        cost_usd: 0.0217
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Bounded OpenAI challenger after official docs and
          `python scripts/discover-models.py --check-new` confirmed the GPT-5.6
          Sol/Terra/Luna tiers. Promptfoo 0.121.1's built-in OpenAI Responses
          provider sent chat-style image content items that the Responses API
          rejected, so the run used
          `benchmarks/providers/openai_responses_model.py` with
          `OPENAI_RESPONSES_MODEL=gpt-5.6-sol`,
          `OPENAI_RESPONSES_REASONING_EFFORT=none`, and current Sol token
          prices supplied by environment variables. Result artifact:
          `benchmarks/results/gpt56-sol-image-crop-extraction-20260711.json`.
          The run completed `13/13` with `0` provider errors, `overall =
          0.9403`, average latency about `3223 ms`, `51027` prompt tokens,
          `920` completion tokens, and estimated Sol cost about `$0.2827` total
          (`$0.0217` per case). This clears the detector target but does not
          replace the maintained Gemini 3 Flash detector because quality is
          below the current `0.9703` score and cost is higher. Portable tracked
          proof: `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.6 Terra Responses (conservative-count prompt, reasoning none)"
        metrics:
          overall: 0.9723
          pass_rate: 1.0
          cases_total: 13
          latency_ms: 2641
        latency_ms: 2641
        cost_usd: 0.0109
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Bounded GPT-5.6 Terra detector challenger through
          `benchmarks/providers/openai_responses_model.py` after official docs
          and model discovery confirmed the tier. Result artifact:
          `benchmarks/results/gpt56-terra-image-crop-extraction-20260711.json`.
          The run completed `13/13` with `0` provider errors, `overall =
          0.9723`, average latency about `2641 ms`, `51027` prompt tokens,
          `936` completion tokens, and estimated Terra cost about `$0.1416`
          total (`$0.0109` per case). This is the best GPT-5.6 detector result
          and slightly beats the maintained Gemini 3 Flash score (`0.9723` vs
          `0.9703`), but the gain is tiny and costs more than 2x the maintained
          proof (`$0.1416` vs about `$0.059` total). Keep it as challenger
          evidence rather than changing the maintained detector by default.
          Portable tracked proof: `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.6 Luna Responses (conservative-count prompt, reasoning none)"
        metrics:
          overall: 0.9623
          pass_rate: 1.0
          cases_total: 13
          latency_ms: 2187
        latency_ms: 2187
        cost_usd: 0.0043
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Bounded GPT-5.6 Luna detector challenger through
          `benchmarks/providers/openai_responses_model.py` after official docs
          and model discovery confirmed the tier. Result artifact:
          `benchmarks/results/gpt56-luna-image-crop-extraction-20260711.json`.
          The run completed `13/13` with `0` provider errors, `overall =
          0.9623`, average latency about `2187 ms`, `51027` prompt tokens,
          `906` completion tokens, and estimated Luna cost about `$0.0565` total
          (`$0.0043` per case). Luna is cheaper and faster than the maintained
          detector proof in this pass, but quality is below both Terra and the
          maintained Gemini 3 Flash score, so it does not replace the detector.
          Portable tracked proof: `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "Grok 4.5 Responses (conservative-count prompt, reasoning low)"
        metrics:
          overall: 0.8026
          pass_rate: 0.9231
          cases_total: 13
          latency_ms: 2433
        latency_ms: 2433
        cost_usd: 0.00564
        cost_estimated: false
        measured: 2026-07-20
        git_sha: ac574bf
        note: >
          Bounded direct-xAI Responses challenger after official docs and
          authenticated HTTP 200 model/text probes confirmed `grok-4.5`.
          `benchmarks/providers/xai_grok_responses.py` normalized the maintained
          multimodal prompts and recorded xAI-reported usage/cost. Result:
          `benchmarks/results/grok45-low-image-crop-extraction-20260720.json`.
          The run completed `12/13` with `0` provider errors, `overall =
          0.8026`, average latency about `2433 ms`, and reported cost about
          `$0.07328` total (`$0.00564` per case). Manual source inspection
          classified `Image011` as model-wrong: Grok cropped only the embossed
          seal instead of the maintained seal-plus-signatures visual region. A
          high-reasoning failed-case retry remained wrong at `0.5095`, so no
          full high-reasoning or page-context run was warranted. Portable proof:
          `docs/evals/attempts/016-grok45-bounded-challenger.md`.
      - model: "Gemini 3 Flash (conservative-count prompt, heading-safe revision)"
        metrics:
          overall: 0.9678
          pass_rate: 1.0
        measured: 2026-04-03
        git_sha: b1a9964
        note: >
          After tightening `crop-conservative-count.js` to exclude ordinary
          heading-style display text while still allowing standalone text-as-art,
          a fresh rerun via
          `benchmarks/results/single-model-crop-detection-g3flash-20260403-promptfix.json`
          reached `13/13` scorer passes and `0.9678 / 1.0`
          (overall / pass_rate). The previously ambiguous mixed photo/title
          pages now align with the maintained goldens; the only materially weak
          remaining case is `Image000`, where the full-cover crop is still
          slightly inset but above the deletion-gate aggregate.
      - model: "Gemini 3 Flash (conservative-count prompt)"
        metrics:
          overall: 0.9321
          pass_rate: 1.0
        measured: 2026-04-03
        git_sha: b1a9964
        note: >
          Fresh rerun of the maintained task filtered to
          `google:gemini-3-flash-preview` + `conservative-count` wrote
          `benchmarks/results/single-model-crop-detection-g3flash-20260403.json`
          with `13/13` scorer passes and `0.9321 / 1.0`
          (overall / pass_rate). Weak cases cluster around extra stylized-title
          crops on `Image011`, `Image013`, `Image021`, `Image037`, and
          `Image059`; those were classified as ambiguous prompt/golden tension,
          while `Image000` remains model-wrong on full-cover coverage.
      - model: "Gemini 3 Flash (conservative-count prompt)"
        metrics:
          overall: 0.918
          pass_rate: 1.0
        measured: 2026-04-03
        note: "Fresh rerun on the repaired maintained C4 surface after restoring `conservative-count` to `tasks/image-crop-extraction.yaml`."
      - model: "Gemini 3 Pro (detector) + Gemini 2.5 Flash (validator)"
        metrics:
          overall: 0.856
          pass_rate: 0.77
        measured: 2026-01-25
        note: "Previous winner. Two-stage detector-validator with auto-retry on count mismatch."
      - model: "Gemini 2.5 Pro"
        metrics:
          overall: 0.78
        measured: 2026-01-24
        note: "Baseline eval before model sweep"
      - model: "Claude Sonnet 4.5"
        metrics:
          overall: 0.72
        measured: 2026-01-24
        note: "Struggled with multi-illustration pages"
      - model: "GPT-5.1"
        metrics:
          overall: 0.68
        measured: 2026-01-24
        note: "Poor bounding box precision"

    attempts:
      - id: "001"
        date: 2026-01-24
        status: succeeded
        approach: "Multi-model sweep with promptfoo (7 models × 3 prompt variants)"
        score_before: null
        score_after: 0.856
        note: "Gemini 3 Pro with baseline prompt won. Simpler prompts beat strict-exclude."
      - id: "002"
        date: 2026-01-25
        status: succeeded
        approach: "Added validator stage (Gemini 2.5 Flash) and auto-retry on count mismatch"
        score_before: 0.856
        score_after: 0.856
        note: "Score unchanged but text contamination eliminated in production crops"
      - id: "003"
        date: 2026-03-11
        status: succeeded
        approach: "Story 133: Gemini 3 Flash + conservative-count prompt + axis-swap fix + token budget increase"
        score_before: 0.856
        score_after: 0.900
        note: "5.7x cost reduction ($1.25→$0.22/book). 20/20 golden crops in production pipeline."
      - id: "004"
        date: 2026-04-03
        status: partial
        approach: "Story 183 restored the checked-in crop promptfoo fixture corpus and reran a clean-checkout detector smoke"
        note: >
          Restored the canonical local `.b64.txt` fixtures under
          `benchmarks/input/source-pages-b64/` and
          `benchmarks/input/crop-validation-b64/`, then added focused
          substrate integrity coverage in
          `tests/test_crop_benchmark_substrate.py`. Fresh clean-checkout smoke
          via `cd benchmarks && promptfoo eval -c tasks/image-crop-extraction.yaml
          --no-cache --filter-first-n 1 -j 1 --no-write` progressed past the
          old missing-file failure and entered provider execution, which is
          enough to prove the benchmark substrate is now locally runnable. This
          pass did not record a new comparable detector leaderboard score; the
          fresh current-pass recorded crop score landed on the dedicated
          `crop-validation` surface instead.
      - id: "005"
        date: 2026-04-03
        status: succeeded
        approach: "Follow-up `/improve-eval image-crop-extraction`: restore `conservative-count` to the maintained task and rerun Gemini 3 Flash on the repaired C4 surface"
        score_before: 0.900
        score_after: 0.918
        note: >
          Classification: task-config wrong, not golden-wrong. The maintained
          `image-crop-extraction` task had drifted to a three-prompt surface
          even though the registry/runbook claimed the winning
          `conservative-count` prompt was current. Fresh drifted-task baseline:
          `baseline = 0.848 / 0.769`, `strict-exclude = 0.915 / 0.846`,
          `two-step = 0.827 / 0.769` (overall / pass_rate). After restoring
          `conservative-count`, maintained-task reruns on 2026-04-03 reached
          `0.918 / 1.0` on the 13-page corpus, with manual inspection of
          `Image000`, `Image001`, and `Image011`.
      - id: "006"
        date: 2026-04-03
        status: partial
        approach: "Follow-up `/improve-eval single-model-crop-detection`: rerun the maintained single-stage Gemini 3 Flash candidate on the repaired C4 surface"
        score_before: 0.918
        score_after: 0.9321
        note: >
          Comparable fresh rerun on `b1a9964` via
          `promptfoo eval -c tasks/image-crop-extraction.yaml --no-cache
          --filter-providers 'google:gemini-3-flash-preview'
          --filter-prompts 'conservative-count' --output
          results/single-model-crop-detection-g3flash-20260403.json -j 1`
          improved the maintained single-stage candidate from `0.918 / 1.0` to
          `0.9321 / 1.0`. Mismatch classification: `Image000` stayed
          model-wrong because the full-cover crop remains inset; `Image011`,
          `Image013`, `Image021`, `Image037`, and `Image059` are ambiguous
          because the maintained prompt says stylized title text counts as a
          visual element while current goldens only count the photographs on
          those pages. No golden changes were made in this pass because that
          would alter more than 5% of the corpus.
      - id: "007"
        date: 2026-04-03
        status: succeeded
        approach: "Refine the maintained `conservative-count` prompt to exclude heading-style display text, then rerun Gemini 3 Flash on the repaired C4 surface"
        score_before: 0.9321
        score_after: 0.9678
        note: >
          Classification: prompt-wrong, not golden-wrong. Source-page review of
          `Image011`, `Image013`, `Image021`, `Image037`, and `Image059` showed
          the extra crops were ordinary page headings or certificate title lines,
          not standalone visual text elements in the Story 133 sense. Tightening
          `benchmarks/prompts/crop-conservative-count.js` to exclude display text
          that belongs to the reading flow produced
          `benchmarks/results/single-model-crop-detection-g3flash-20260403-promptfix.json`
          with `0.9678 / 1.0`, clearing both the maintained quality surface and
          the C4 deletion gate without any golden edits.
      - id: "008"
        date: 2026-04-10
        status: succeeded
        approach: "Story 207: rerun the maintained detector surface and refresh the canonical coverage rows from current-pass proof"
        score_before: 0.9678
        score_after: 0.9703
        note: >
          Fresh current-pass rerun via `promptfoo eval -c
          tasks/image-crop-extraction.yaml --no-cache --filter-providers
          'google:gemini-3-flash-preview' --filter-prompts
          'conservative-count' --output
          results/story207-image-crop-baseline.json -j 1` completed with
          `13/13` scorer passes, `0` failures, `0` errors, average latency
          about `7878 ms` per page, and total eval cost about `$0.059`.
          Recomputing the mean from the per-case scores yielded `0.9703 / 1.0`
          (overall / pass_rate). `Image000` remains the weakest case at
          `0.8164`, but the maintained aggregate still clears the crop gate and
          now anchors the canonical coverage-matrix rows. Portable tracked proof:
          `docs/evals/attempts/001-image-crop-extraction-story207-proof-refresh.md`.
      - id: "009"
        date: 2026-04-24
        status: failed
        approach: "New-subject-model screen: add callable GPT-5.5 to the maintained detector task and rerun the winning conservative-count slice"
        score_before: 0.9703
        score_after: 0.8111
        note: >
          `gpt-5.5` is callable through the promptfoo OpenAI chat provider, but
          its first maintained detector pass underperformed the current Gemini
          3 Flash baseline. The failures were model-wrong bbox precision
          misses: `Image001` cropped only the stylized title text instead of
          the full title/title-caption area, `Image013` undercovered the
          portrait, and `Image020` undercovered the second ranch photo. An
          attempted `gpt-5.5-pro` chat-provider row returned 404 "not a chat
          model", so it was not kept in the maintained promptfoo config. No
          prompt, scorer, or golden changes were made.
      - id: "010"
        date: 2026-04-24
        status: failed
        approach: "Re-run the detector through OpenAI Responses after GPT-5.5 Pro became Responses-callable"
        score_before: 0.8111
        score_after: 0.791
        note: >
          Added Responses-compatible OpenAI image blocks and reran
          `openai:responses:gpt-5.5` on the same maintained
          `conservative-count` detector slice. The direct Responses path still
          failed `3/13` cases and scored slightly lower than the earlier Chat
          run (`0.791` vs `0.8111`), though the failure set changed. GPT-5.5
          Pro Responses passed a one-case `Image000` smoke, but full detector
          attempts did not complete in the interactive promptfoo loop; `Image001`
          appears pathological for Pro-medium under this prompt. No scorer or
          golden changes were made.
      - id: "011"
        date: 2026-05-06
        status: failed
        approach: "Screen OpenAI `chat-latest` / GPT-5.5 Instant as a bounded detector challenger after the 2026-05-05 release"
        score_before: 0.9703
        score_after: 0.8006
        note: >
          `chat-latest` is present in the OpenAI model list and is image-callable
          through the local Responses shim, but it did not beat the maintained
          Gemini 3 Flash detector. The run scored `0.8006` with `11/13` passes,
          `0` provider errors, average latency about `3610 ms`, and estimated
          cost about `$0.1389` total. `Image013` and `Image021` failed due to
          undercovered portrait/photo bounding boxes. No maintained providers,
          prompts, scorers, or goldens were changed.
      - id: "012"
        date: 2026-05-20
        status: failed
        approach: "Screen Kimi K2.6 through Moonshot's direct API as a bounded detector challenger"
        score_before: 0.9703
        score_after: 0.8981
        note: >
          Kimi K2.6 is image-callable through the local Moonshot promptfoo
          provider, but it did not beat the maintained Gemini 3 Flash detector.
          The run scored `0.8981` with `12/13` passes, `0` provider errors,
          average latency about `6176 ms`, and estimated cost about `$0.0519`
          total. The miss was model-wrong on `Image001`: the maintained
          prompt/golden count the stylized title page as a standalone visual
          element, while Kimi returned `{"images": []}`. Thinking mode did not
          repair that failed case. No maintained providers, prompts, scorers,
          or goldens were changed.
      - id: "013"
        date: 2026-05-20
        status: failed
        approach: "Screen Google `gemini-3.5-flash` as a bounded detector challenger after the Gemini 3.5 Flash release"
        score_before: 0.9703
        score_after: 0.9679
        note: >
          `gemini-3.5-flash` is live in the Gemini model list and callable
          through promptfoo once stale `GOOGLE_API_KEY` is unset in favor of the
          repo-scoped `DOC_WEB_GEMINI_API_KEY`. The maintained
          `conservative-count` detector slice passed at `13/13` with `0`
          provider errors and `overall = 0.9679`, but that is still slightly
          below the maintained Gemini 3 Flash `0.9703` result and was slower in
          this pass. Estimated paid standard pricing is also higher than the
          maintained detector proof (`$0.1095` total vs about `$0.059`). No
          maintained providers, prompts, scorers, or goldens were changed. The
          follow-up crop-only validator and page-context gate later ran after
          paid-tier availability recovered, scoring `39/40` and `21/22`
          respectively. The bounded handwritten OCR screen reached only
          `0.247350` on the corrected real-fixture floor. Result: Gemini 3.5
          Flash is callable and useful evidence, but it does not replace any
          maintained detector, validator, or OCR provider. Portable tracked
          proof:
          `docs/evals/attempts/011-gemini35-flash-bounded-challenger.md`.
      - id: "014"
        date: 2026-06-30
        status: failed
        approach: "Screen Claude Sonnet 5 as a bounded detector challenger after the Anthropic release"
        score_before: 0.9703
        score_after: 0.5334
        note: >
          Claude Sonnet 5 is callable through the same direct Anthropic
          no-sampling provider pattern as Opus 4.8 and Fable 5, but it did not
          preserve the maintained detector contract. The run scored `5/13`
          with `0` provider errors, `overall = 0.5334`, average latency about
          `3834 ms`, and estimated cost about `$0.1279` total. The failures
          were bbox/coordinate-quality misses rather than access or parser
          failures, including normalized/scale-incompatible boxes on portrait
          pages such as `Image013`. No maintained providers, prompts, scorers,
          or goldens were changed. Portable tracked proof:
          `docs/evals/attempts/014-sonnet5-bounded-challenger.md`.
      - id: "015"
        date: 2026-07-11
        status: partial
        approach: "Screen GPT-5.6 Sol, Terra, and Luna as bounded detector challengers after the OpenAI release"
        score_before: 0.9703
        score_after: 0.9723
        note: >
          Official OpenAI docs and `python scripts/discover-models.py
          --check-new` confirmed the new GPT-5.6 Sol/Terra/Luna tiers and model
          slugs. Because promptfoo 0.121.1's built-in Responses provider sent
          unsupported chat-style image content blocks, the sweep used
          `benchmarks/providers/openai_responses_model.py` against the maintained
          `conservative-count` detector slice. Terra produced the best GPT-5.6
          detector result at `13/13`, `overall = 0.9723`, `0` provider errors,
          average latency about `2641 ms`, and estimated cost about `$0.1416`
          total. This narrowly beats the maintained Gemini 3 Flash score but is
          more than 2x the maintained proof cost, and the companion page-context
          sweep failed the hard deletion-gate contract. No maintained providers,
          prompts, scorers, or goldens were changed. Portable tracked proof:
          `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - id: "016"
        date: 2026-07-20
        status: failed
        approach: "Screen Grok 4.5 through xAI's direct Responses API as a bounded detector challenger"
        score_before: 0.9703
        score_after: 0.8026
        note: >
          Authenticated first-party model and Responses probes succeeded, and a
          one-case vision smoke produced valid scored output. The full
          low-reasoning maintained detector run scored `0.8026`, passed `12/13`
          cases, averaged about `2433 ms`, and cost `$0.07328` total from xAI's
          reported usage. Manual inspection classified the `Image011` miss as
          model-wrong bbox undercoverage; a high-reasoning failed-case retry
          repeated the same seal-only crop. No prompts, scorers, goldens, or
          maintained providers changed. Portable proof:
          `docs/evals/attempts/016-grok45-bounded-challenger.md`.
      - id: "017"
        date: 2026-07-21
        status: failed
        approach: "Screen Gemini 3.6 Flash and Gemini 3.5 Flash-Lite as exact-ID GA detector challengers"
        score_before: 0.9703
        score_after: 0.9692
        note: >
          Gemini 3.6 Flash reached `13/13` and `0.9692`, narrowly below the
          maintained score and at higher estimated cost. Flash-Lite's initial
          prompt-only run reached `8/13`; the output-contract correction is
          recorded in the next attempt. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - id: "018"
        date: 2026-07-21
        status: failed
        approach: "Correct the Flash-Lite detector transport with API-enforced JSON Schema and unambiguous integer bounding boxes"
        score_before: 0.6427
        score_after: 0.8988
        note: >
          The strict schema raised the candidate from `8/13` to `12/13`,
          eliminated every structural output failure, cut average latency to
          about `1591 ms`, and cost an estimated `$0.0087`. It still missed the
          overall target because `Image121` reduced the lower-left portrait to
          a narrow strip. Manual source inspection confirmed model-wrong, not
          golden/scorer-wrong. Do not replace the maintained detector.
          Portable proof: `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.

    retry_when:
      - condition: new-subject-model
        note: "Gemini 4 or equivalent may push past 0.95 target"
      - condition: golden-fix
        note: "Golden references cover limited page types; add table-heavy pages"

  - id: crop-validation
    name: Crop Validation
    type: quality
    description: >
      Dedicated bounded crop-only text-exclusion / crop-quality surface for the
      maintained 40-crop corpus from the Onward illustration lane. Scores
      pass/fail crop judgments against checked-in labels for external page
      text, excessive blank space, and obvious wrong-region crops.
    pipeline_stage: crop_illustrations
    runner: promptfoo
    command: "cd benchmarks && promptfoo eval -c tasks/crop-validation.yaml --no-cache --filter-providers 'google:gemini-3.1-flash-lite' --filter-prompts 'caption-focus' --output results/crop-validation-story183-g31-caption-focus.json -j 1"
    config: benchmarks/tasks/crop-validation.yaml
    scorer: benchmarks/scorers/crop_validation_scorer.py
    golden: benchmarks/golden/crop-validation.json
    story_refs: ["183", "209"]
    category_refs: ["spec:4", "spec:8"]
    compromise_refs: ["C5"]
    spec_compromise: C5

    target:
      metric: pass_rate
      value: 1.0
      overall: 1.0
      note: >
        This is the dedicated current C5-linked truth surface for the checked-in
        40-crop corpus. Passing here does not by itself delete C5; the spec
        still calls for broader page-level evidence before the trim heuristics
        can be removed.

    scores:
      - model: "Gemini 3.6 Flash (caption-focus prompt)"
        metrics: {overall: 0.975, pass_rate: 0.975, cases_total: 40, latency_ms: 4145}
        cost_usd: 0.1730
        cost_estimated: true
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Completed `39/40`; manual inspection confirmed the sole
          `page-126-000` false negative contains external page text. The
          maintained Gemini 3.1 Flash Lite remains `40/40`.
      - model: "Gemini 3.5 Flash-Lite (caption-focus prompt)"
        metrics: {overall: 0.975, pass_rate: 0.975, cases_total: 40, latency_ms: 4184}
        cost_usd: 0.0230
        cost_estimated: true
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Completed `39/40` with the same manually confirmed model-wrong
          `page-126-000` false negative. It does not replace the maintained
          `40/40` crop-only validator. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "Gemini 3.1 Flash Lite (caption-focus prompt, story209 baseline rerun)"
        metrics:
          overall: 1.0
          pass_rate: 1.0
          cases_total: 40
          latency_ms: 3150
        measured: 2026-04-11
        note: >
          Story 209 reran the maintained bounded crop-only surface via
          `cd benchmarks && promptfoo eval -c tasks/crop-validation.yaml
          --no-cache --filter-providers 'google:gemini-3.1-flash-lite'
          --filter-prompts 'caption-focus' --output
          results/story209-crop-validation-baseline.json -j 1`.
          The rerun completed `40/40` with `0` failures, `0` errors, duration
          about `126000 ms`, and total tokens `59086`. Manual inspection in the
          same pass reconfirmed that `page-012-001` still labels as a clean
          pass for integral seal text while `page-122-000` still labels as a
          fail for caption leakage. Story 209 keeps this surface explicitly
          bounded and adds the new page-context deletion gate for the broader
          C5 question.
      - model: "GPT-5.5 (caption-focus prompt)"
        metrics:
          overall: 0.975
          pass_rate: 0.975
          cases_total: 40
          latency_ms: 6419
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh new-subject-model challenger rerun via
          `cd benchmarks && promptfoo eval -c tasks/crop-validation.yaml
          --no-cache --filter-providers 'openai:chat:gpt-5.5'
          --filter-prompts 'caption-focus' --output
          results/gpt55-crop-validation-20260424.json -j 1`. The run
          completed `39/40` with `1` scorer failure, `0` errors,
          `overall = 0.975`, `pass_rate = 0.975`, average latency about
          `6419 ms`, and total tokens `242008`. Estimated current GPT-5.5 API
          cost from token counts is about `$1.3258` total (`$0.0331` per case).
          The miss was `page-126-000`, where GPT-5.5 passed a crop with visible
          external page text at the left edge by treating the text as plaque
          text inside the photo. Portable tracked proof:
          `docs/evals/attempts/003-gpt55-crop-eval-refresh.md`.
      - model: "GPT-5.5 Responses (caption-focus prompt)"
        metrics:
          overall: 0.975
          pass_rate: 0.975
          cases_total: 40
          latency_ms: 4396
        latency_ms: 4396
        cost_usd: 0.035
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Direct Responses API rerun via
          `cd benchmarks && promptfoo eval -c tasks/crop-validation.yaml
          --no-cache --filter-providers '^openai:responses:gpt-5\.5$'
          --filter-prompts 'caption-focus' --output
          results/gpt55-responses-crop-validation-20260424.json -j 1`.
          The run matched the earlier Chat result at `39/40` with the same
          false negative on `page-126-000`, `0` provider errors, average
          latency about `4396 ms`, and estimated current API cost about
          `$1.4005` total (`$0.0350` per case). Portable tracked proof:
          `docs/evals/attempts/006-gpt55-responses-eval-refresh.md`.
      - model: "GPT-5.5 Pro Responses (caption-focus prompt)"
        metrics:
          overall: 0.975
          pass_rate: 0.975
          cases_total: 40
          latency_ms: 17359
        latency_ms: 17359
        cost_usd: 0.2305
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          GPT-5.5 Pro Responses rerun via
          `cd benchmarks && PROMPTFOO_EVAL_TIMEOUT_MS=180000 promptfoo eval -c
          tasks/crop-validation.yaml --no-cache --filter-providers
          '^openai:responses:gpt-5\.5-pro$' --filter-prompts
          'caption-focus' --output
          results/gpt55pro-responses-crop-validation-20260424.json -j 1`.
          The run also reached `39/40` with the same false negative on
          `page-126-000`, `0` provider errors, average latency about
          `17359 ms`, and estimated current API cost about `$9.2181` total
          (`$0.2305` per case). Portable tracked proof:
          `docs/evals/attempts/006-gpt55-responses-eval-refresh.md`.
      - model: "Gemini 3.5 Flash (caption-focus prompt, direct REST)"
        metrics:
          overall: 0.975
          pass_rate: 0.975
          cases_total: 40
          latency_ms: 3767
        latency_ms: 3767
        cost_usd: 0.0053
        cost_estimated: true
        measured: 2026-05-20
        git_sha: 99450a3
        note: >
          Bounded Gemini 3.5 Flash crop-only validator retry after paid-tier
          availability recovered. Direct Gemini REST fallback reused the
          maintained prompt text and Python scorer because promptfoo provider
          attempts had first been blocked by free-tier project quota and then by
          `503 UNAVAILABLE` high-demand responses. Result artifact:
          `benchmarks/results/gemini35-flash-crop-validation-direct-20260520.json`.
          The run completed `39/40` with `0` provider errors, `overall = 0.975`,
          average latency about `3767 ms`, and `71027` total tokens (`57182`
          prompt, `2713` candidate, `11132` thinking). Estimated paid standard
          cost is about `$0.2104` total (`$0.0053` per case). The miss was
          model-wrong on `page-126-000`: the golden fails the crop because
          book-page text is visible along the left side, but Gemini 3.5 Flash
          treated the text as integral plaque text. This ties recent GPT-5.5
          style `39/40` challengers but does not replace the maintained Gemini
          3.1 Flash Lite `40/40` crop-only validator. Portable tracked proof:
          `docs/evals/attempts/011-gemini35-flash-bounded-challenger.md`.
      - model: "Gemini 3.1 Flash Lite (caption-focus prompt)"
        metrics:
          overall: 1.0
          pass_rate: 1.0
          cases_total: 40
          latency_ms: 3021
        measured: 2026-04-03
        note: >
          Story 183 repaired the tracked crop benchmark substrate by restoring
          the canonical local `.b64.txt` fixtures under
          `benchmarks/input/source-pages-b64/` and
          `benchmarks/input/crop-validation-b64/`, then reran the full 40-crop
          dedicated validation surface via
          `cd benchmarks && promptfoo eval -c tasks/crop-validation.yaml
          --no-cache --filter-providers 'google:gemini-3.1-flash-lite'
          --filter-prompts 'caption-focus' --output
          results/crop-validation-story183-g31-caption-focus.json -j 1`.
          The repaired local benchmark completed `40/40` with `0` failures,
          `0` errors, `120833 ms` total duration, and `59087` total tokens.
          Manual inspection in the same pass confirmed the 4 checked fail cases
          (`page-018-000`, `page-092-000`, `page-122-000`, `page-126-000`)
          still returned `{"verdict": "fail", ...}` while integral-text pass
          case `page-012-000` still returned `{"verdict": "pass", ...}` in
          `benchmarks/results/crop-validation-story183-g31-caption-focus.json`.

    attempts:
      - id: "001"
        date: 2026-04-03
        status: succeeded
        approach: "Story 183 repaired the local crop fixture corpus, added integrity coverage, and reran the dedicated C5-linked crop-validation surface on Gemini 3.1 Flash Lite + caption-focus"
        score_before: null
        score_after: 1.0
        note: >
          Before the repair, a clean-checkout smoke failed before any provider
          call because `tasks/crop-validation.yaml` could not resolve
          `../input/crop-validation-b64/page-001-000.b64.txt`. After restoring
          the canonical tracked fixtures and adding
          `tests/test_crop_benchmark_substrate.py`, the same surface completed
          the full 40-crop corpus with `40/40` correct pass/fail judgments and
          no provider-side errors.
      - id: "002"
        date: 2026-04-11
        status: succeeded
        approach: "Story 209 baseline rerun of the bounded crop-only surface before adding the broader page-context deletion gate"
        score_before: 1.0
        score_after: 1.0
        note: >
          Fresh current-pass rerun on the maintained `caption-focus` slice
          completed `40/40` with `0` failures, `0` errors, duration about
          `126000 ms`, and total tokens `59086`. The result confirmed the
          existing crop-only surface remained healthy and that the missing
          truth was broader page context, not a broken bounded validator.
      - id: "003"
        date: 2026-04-24
        status: failed
        approach: "New-subject-model screen: add callable GPT-5.5 to the bounded crop-only validation task and rerun caption-focus"
        score_before: 1.0
        score_after: 0.975
        note: >
          GPT-5.5 reached `39/40` but did not match the maintained Gemini 3.1
          Flash Lite `40/40` result. Mismatch classification: model-wrong false
          negative on `page-126-000`; manual image inspection confirmed the
          crop includes external page text on the left side even though the
          model passed it as integral plaque text. No golden changes were made.
      - id: "004"
        date: 2026-04-24
        status: failed
        approach: "Re-run crop-only validation through OpenAI Responses for GPT-5.5 and GPT-5.5 Pro"
        score_before: 0.975
        score_after: 0.975
        note: >
          Direct Responses API did not change the outcome. GPT-5.5 Responses
          and GPT-5.5 Pro Responses both scored `39/40` and both missed
          `page-126-000` as a false negative. Pro was much slower and more
          expensive with no quality gain. No prompt, scorer, or golden changes
          were made.
      - id: "005"
        date: 2026-05-20
        status: failed
        approach: "Screen Gemini 3.5 Flash as a bounded crop-only validator challenger after paid-tier availability recovered"
        score_before: 1.0
        score_after: 0.975
        note: >
          Initial promptfoo and direct REST attempts were blocked first by
          free-tier project quota and then by repeated `503 UNAVAILABLE`
          high-demand responses. After the heartbeat retry health probe
          succeeded on the canonical doc-web key with `serviceTier: standard`, I
          reran the 40-case crop-only validator through the direct REST fallback.
          The run scored `39/40` with `0` provider errors and only missed
          `page-126-000`, where Gemini 3.5 Flash treated visible external
          book-page text as integral plaque text. No maintained providers,
          prompts, scorers, or goldens were changed.
      - id: "006"
        date: 2026-07-21
        status: failed
        approach: "Screen Gemini 3.6 Flash and Gemini 3.5 Flash-Lite on the maintained crop-only validator surface"
        score_before: 1.0
        score_after: 0.975
        note: >
          Both candidates completed `39/40` and missed `page-126-000`; manual
          source/crop inspection confirmed the golden is correct. No maintained
          surface changed. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.

    retry_when:
      - condition: new-subject-model
        note: >
          Rerun when a new validator model or prompt looks strong enough to
          widen or replace the current bounded C5-linked truth surface.
      - condition: new-approach
        note: >
          Broaden or redesign this surface when the project is ready to measure
          the spec's wider page-level text-exclusion deletion gate instead of
          only the current 40-crop corpus.
      - condition: golden-fix
        note: >
          Rerun if the checked-in crop labels or fail reasons change.

  - id: crop-page-level-deletion-gate
    name: Crop Page-Level Deletion Gate
    type: quality
    description: >
      Page-context C5 deletion-gate surface for the maintained runtime overlap
      corpus. Shows the source page plus the extracted crop together and scores
      pass/fail crop judgments against checked-in labels for external page
      text, neighboring-visual leakage, excessive blank space, and integral
      image text.
    pipeline_stage: crop_illustrations
    runner: promptfoo
    command: "cd benchmarks && ../scripts/run_with_doc_web_env.py promptfoo eval -c tasks/crop-page-level-deletion-gate.yaml --no-cache --output results/crop-page-level-deletion-gate-gpt55-responses-current-20260424.json -j 1"
    config: benchmarks/tasks/crop-page-level-deletion-gate.yaml
    scorer: benchmarks/scorers/crop_validation_scorer.py
    golden: benchmarks/golden/crop-page-level-deletion-gate.json
    story_refs: ["209"]
    category_refs: ["spec:4", "spec:8"]
    compromise_refs: ["C5"]
    spec_compromise: C5

    ranking:
      quality_winner: "GPT-5.5 Responses (page-context prompt, promptfix)"
      applied: >
        The maintained promptfoo task now runs `openai:responses:gpt-5.5` by
        default because it is the only current validator that reaches `22/22`
        on the corrected page-context golden. Gemini 3.1 Flash Lite remains
        useful as a historical/regression comparison, but its fresh
        corrected-golden rerun is `21/22`.
      note: >
        Do not switch the crop-only `crop-validation` surface from Gemini 3.1
        Flash Lite; GPT-5.5 Responses is only the current winner for the
        broader page-context deletion gate.

    target:
      metric: pass_rate
      value: 1.0
      overall: 1.0
      note: >
        This is the current page-context C5 deletion-gate truth surface for the
        maintained runtime overlap corpus. Passing here means the validator
        matches human pass/fail labels on `22` checked cases; it does not
        delete `C5` because that corpus still contains `5` explicit fail-labeled
        residue cases (`page-018-000`, `page-092-000`, `page-122-000`,
        `page-122-001`, and `page-126-000`).

    scores:
      - model: "Gemini 3.6 Flash (page-context prompt)"
        metrics:
          overall: 0.9545
          pass_rate: 0.9545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4569
        cost_usd: 0.1532
        cost_estimated: true
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Completed `21/22`; manual inspection confirmed `page-122-001`
          leaks a neighboring portrait slice even though the candidate passed
          it. The maintained GPT-5.5 Responses validator remains `22/22`.
          Portable proof: `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "Gemini 3.1 Flash Lite (page-context prompt)"
        metrics:
          overall: 1.0
          pass_rate: 1.0
          cases_total: 22
          pass_labeled: 18
          fail_labeled: 4
          latency_ms: 2812
        measured: 2026-04-11
        note: >
          Story 209 added `benchmarks/tasks/crop-page-level-deletion-gate.yaml`
          plus the supporting overlap corpus and reran the maintained
          page-context surface via
          `cd benchmarks && promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --no-cache --output
          results/story209-crop-page-level-deletion-gate-g31-page-context-v2.json
          -j 1`. After one prompt-tightening pass for `page-126-000`, the final
          rerun completed `22/22` with `0` failures, `0` errors, duration
          `61863 ms`, and total tokens `56945`. Manual inspection in the same
          pass confirmed `page-012-001` still labels as a pass for integral
          seal text while `page-018-000`, `page-122-000`, and `page-126-000`
          all label as fails for external page-text leakage. Portable tracked
          proof: `docs/evals/attempts/002-crop-page-level-deletion-gate-story209-proof.md`.
          This score predates the 2026-04-24 page-context golden correction for
          `page-122-001`; the fresh corrected-golden rerun below supersedes it
          for current comparisons.
      - model: "Gemini 3.1 Flash Lite (page-context prompt, corrected golden)"
        metrics:
          overall: 0.9545
          pass_rate: 0.9545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 5454
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh corrected-golden regression after replacing the revoked shared
          Google credential with `DOC_WEB_GEMINI_API_KEY` and running promptfoo
          through `scripts/run_with_doc_web_env.py`. Command:
          `cd benchmarks && ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --no-cache
          --filter-providers '^google:gemini-3\.1-flash-lite-preview$'
          --output
          results/g31-crop-page-level-deletion-gate-promptfix-rerun-20260424.json
          -j 1`. The run completed `21/22` with `1` scorer failure, `0`
          provider errors, average latency about `5454 ms`, and total tokens
          `60728`. The remaining miss is model-wrong: Gemini passed
          `page-122-001` even though the corrected page-context golden now fails
          it for leaking a neighboring portrait slice. Portable tracked proof:
          `docs/evals/attempts/008-gemini-page-context-corrected-golden-rerun.md`.
      - model: "GPT-5.5 (page-context prompt)"
        metrics:
          overall: 0.7273
          pass_rate: 0.7273
          cases_total: 22
          pass_labeled: 18
          fail_labeled: 4
          latency_ms: 12484
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh new-subject-model challenger rerun via
          `cd benchmarks && promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --no-cache
          --filter-providers 'openai:chat:gpt-5.5' --output
          results/gpt55-crop-page-level-deletion-gate-20260424.json -j 1`.
          The run completed `16/22` with `6` scorer failures, `0` errors,
          `overall = 0.7273`, `pass_rate = 0.7273`, average latency about
          `12484 ms`, and total tokens `233193`. Estimated current GPT-5.5 API
          cost from token counts is about `$1.3984` total (`$0.0636` per case).
          Failures were false positives on `page-004-000`, `page-012-001`,
          `page-021-000`, `page-021-001`, and `page-122-001`, plus the same
          false negative on `page-126-000`. Portable tracked proof:
          `docs/evals/attempts/003-gpt55-crop-eval-refresh.md`.
      - model: "GPT-5.5 Responses (page-context prompt)"
        metrics:
          overall: 0.9091
          pass_rate: 0.9091
          cases_total: 22
          pass_labeled: 18
          fail_labeled: 4
          latency_ms: 5006
        latency_ms: 5006
        cost_usd: 0.0588
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Direct Responses API rerun via
          `cd benchmarks && promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --no-cache
          --filter-providers '^openai:responses:gpt-5\.5$' --output
          results/gpt55-responses-crop-page-level-deletion-gate-20260424.json
          -j 1`. The run improved materially over the earlier Chat result
          (`20/22` vs `16/22`) but still failed the maintained gate. Remaining
          failures were reported as `page-021-001` and `page-122-001`; the
          later prompt-repair pass confirmed `page-021-001` as a prompt false
          positive and reclassified `page-122-001` as a page-context golden
          error because the crop includes a neighboring portrait slice.
          Estimated current API cost was about `$1.2926` total (`$0.0588` per
          case). Portable tracked proof:
          `docs/evals/attempts/006-gpt55-responses-eval-refresh.md`.
      - model: "GPT-5.5 Responses (page-context prompt, promptfix)"
        metrics:
          overall: 1.0
          pass_rate: 1.0
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 5253
        latency_ms: 5253
        cost_usd: 0.0595
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Follow-up prompt repair after direct Responses measurement. The
          prompt now treats sky/snow/photo backdrops as image content, states
          that this gate primarily checks page-text and neighboring-visual
          leakage, and tolerates tight line-art framing unless the full-page
          context shows a large missing part of the intended visual. Manual
          inspection also corrected the `page-122-001` page-context golden from
          pass to fail because the crop leaks into the neighboring Sophie
          L'Heureux portrait. After narrowing the maintained task to the winning
          provider, the applied rerun
          `cd benchmarks && ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --no-cache --output
          results/crop-page-level-deletion-gate-gpt55-responses-current-20260424.json
          -j 1` completed `22/22` with `0` failures, `0` errors, average
          latency about `5253 ms`, and estimated current API cost about
          `$1.3094` total (`$0.0595` per case). A later Gemini regression check
          with `DOC_WEB_GEMINI_API_KEY` succeeded but scored `21/22` on the
          corrected golden, so GPT-5.5 Responses is the only current `22/22`
          page-context validator. Portable tracked proof:
          `docs/evals/attempts/009-gpt55-page-context-applied-default.md`.
      - model: "OpenAI chat-latest Responses alias (GPT-5.5 Instant, page-context prompt)"
        metrics:
          overall: 0.9545
          pass_rate: 0.9545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 5698
        latency_ms: 5698
        cost_usd: 0.0188
        cost_estimated: true
        measured: 2026-05-06
        git_sha: 60942e2
        note: >
          Bounded GPT-5.5 Instant alias challenger after verifying
          `chat-latest` can be called with image inputs through
          `benchmarks/providers/openai_chat_latest_responses.py`. Command:
          `cd benchmarks && DOC_WEB_OPENAI_API_KEY="$OPENAI_API_KEY"
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --providers
          "python:$(pwd)/providers/openai_chat_latest_responses.py" --no-cache
          --output
          results/chat-latest-crop-page-level-deletion-gate-20260506.json
          -j 1`. The run completed `21/22` with `0` provider errors,
          `overall = 0.9545`, `pass_rate = 0.9545`, average latency about
          `5698 ms`, and estimated GPT-5.5-rate cost about `$0.4136` total
          (`$0.0188` per case). The single failure was `page-122-001`: the
          model returned `pass` even though the corrected golden fails the crop
          for leaking a neighboring portrait slice. This does not replace the
          maintained `openai:responses:gpt-5.5` page-context winner. Portable
          tracked proof:
          `docs/evals/attempts/010-chat-latest-instant-bounded-challenger.md`.
      - model: "Kimi K2.6 (page-context prompt, non-thinking)"
        metrics:
          overall: 0.9545
          pass_rate: 0.9545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 9868
        latency_ms: 9868
        cost_usd: 0.0075
        cost_estimated: true
        measured: 2026-05-20
        git_sha: 37ef4af
        note: >
          Bounded Moonshot direct-API challenger after verifying Kimi K2.6 can
          handle the repo's two-image full-page-plus-crop prompt shape through
          `benchmarks/providers/moonshot_kimi_chat.py`. Command:
          `cd benchmarks &&
          DOC_WEB_ENV_FILE=/Users/cam/Documents/Projects/doc-web/.env
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --providers
          "python:$(pwd)/providers/moonshot_kimi_chat.py" --output
          results/kimi-k26-crop-page-level-deletion-gate-20260520.json
          --no-cache -j 1`. The run completed `21/22` with `0` provider
          errors, `overall = 0.9545`, `pass_rate = 0.9545`, average latency
          about `9868 ms`, and estimated Kimi K2.6 cost about `$0.1646` total
          (`$0.0075` per case). The miss was model-wrong on `page-122-001`:
          Kimi returned `pass` even though manual source/crop inspection
          confirmed the corrected golden failure for leaking a neighboring
          Sophie L'Heureux portrait slice. A thinking-mode retry of the failed
          case also returned `pass`, so no full thinking-mode rerun was
          warranted. This does not replace the maintained
          `openai:responses:gpt-5.5` page-context winner. Portable tracked
          proof: `docs/evals/attempts/011-kimi-k26-bounded-challenger.md`.
      - model: "Gemini 3.5 Flash (page-context prompt, direct REST)"
        metrics:
          overall: 0.954545
          pass_rate: 0.954545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4554
        latency_ms: 4554
        cost_usd: 0.0084
        cost_estimated: true
        measured: 2026-05-20
        git_sha: 99450a3
        note: >
          Bounded Gemini 3.5 Flash page-context validator retry after paid-tier
          availability recovered. Direct Gemini REST fallback reused the
          maintained page-context prompt text and Python scorer because
          promptfoo provider attempts had first been blocked by free-tier
          project quota and then by `503 UNAVAILABLE` high-demand responses.
          Result artifact:
          `benchmarks/results/gemini35-flash-crop-page-level-deletion-gate-direct-20260520.json`.
          The run completed `21/22` with `0` provider errors,
          `overall = 0.954545`, average latency about `4554 ms`, and `70296`
          total tokens (`59636` prompt, `1533` candidate, `9127` thinking).
          Estimated paid standard cost is about `$0.1854` total (`$0.0084` per
          case). The miss was model-wrong on `page-122-001`: the corrected
          golden fails the crop for a visible neighboring portrait slice, but
          Gemini 3.5 Flash passed it as a clean two-portrait extraction. This
          does not replace the maintained `openai:responses:gpt-5.5`
          page-context winner. Portable tracked proof:
          `docs/evals/attempts/011-gemini35-flash-bounded-challenger.md`.
      - model: "Claude Opus 4.8 (page-context prompt, adaptive high effort)"
        metrics:
          overall: 0.9091
          pass_rate: 0.9091
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4853
        latency_ms: 4853
        cost_usd: 0.0473
        cost_estimated: true
        measured: 2026-05-28
        git_sha: 5254df1
        note: >
          Bounded Anthropic challenger after Conductor Scout 043 and the
          2026-05-28 Anthropic Opus 4.8 release. Promptfoo 0.121.1's built-in
          Anthropic provider sent `temperature`, which Opus 4.8 rejects, so the
          run used the narrow `benchmarks/providers/anthropic_opus48_messages.py`
          provider with sampling params omitted, adaptive thinking enabled, and
          `output_config.effort = high`. Command: `cd benchmarks &&
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --providers
          python:$(pwd)/providers/anthropic_opus48_messages.py --output
          results/opus48-crop-page-level-deletion-gate-20260528.json --no-cache
          -j 1`. The run completed `20/22` with `0` provider errors,
          `overall = 0.9091`, average latency about `4853 ms`, and estimated
          Opus 4.8 cost about `$1.0405` total (`$0.0473` per case). The misses
          were model-wrong false negatives on `page-122-001`, where the crop
          leaks a neighboring portrait slice, and `page-126-000`, where visible
          book-page text appears at the left edge. An `xhigh` failed-case retry
          repaired `0/2`. This does not replace the maintained
          `openai:responses:gpt-5.5` page-context winner. Portable tracked
          proof: `docs/evals/attempts/012-opus48-bounded-challenger.md`.
      - model: "Claude Fable 5 (page-context prompt, adaptive high effort)"
        metrics:
          overall: 0.9091
          pass_rate: 0.9091
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 7423
        latency_ms: 7423
        cost_usd: 0.0961
        cost_estimated: true
        measured: 2026-06-09
        git_sha: 70a65b5
        note: >
          Bounded Anthropic challenger after Conductor Scout 044 and the
          2026-06-09 Claude Fable 5 release. Official Anthropic docs and the
          repo-local `/v1/models` response both confirmed `claude-fable-5` is
          available through the API. The run reused the narrow
          `benchmarks/providers/anthropic_opus48_messages.py` provider with
          sampling params omitted, adaptive thinking enabled, `output_config`
          effort high, and Fable-specific token prices supplied by environment
          variables. Command: `cd benchmarks && ANTHROPIC_OPUS48_MODEL=claude-fable-5
          ANTHROPIC_MESSAGES_INPUT_PRICE_PER_1M=10
          ANTHROPIC_MESSAGES_OUTPUT_PRICE_PER_1M=50
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --providers
          python:$(pwd)/providers/anthropic_opus48_messages.py --output
          results/fable5-crop-page-level-deletion-gate-20260609.json --no-cache
          -j 1`. The run completed `20/22` with `0` provider errors,
          `overall = 0.9091`, average latency about `7423 ms`, `197513`
          prompt tokens, `2771` completion tokens, and estimated Fable cost
          about `$2.1137` total (`$0.0961` per case). The misses were
          model-wrong false negatives on `page-122-001`, where the crop leaks
          the neighboring Sophie L'Heureux portrait, and `page-126-000`, where
          visible adjacent page/plaque text appears at the left edge. Manual
          image inspection confirmed the checked-in golden labels. Because
          Fable fails quality and is also more expensive and slower than the
          maintained GPT-5.5 Responses winner, no higher-effort retry is
          warranted. This does not replace the maintained
          `openai:responses:gpt-5.5` page-context winner. Portable tracked
          proof: `docs/evals/attempts/013-fable5-bounded-challenger.md`.
      - model: "Claude Sonnet 5 (page-context prompt, adaptive high effort)"
        metrics:
          overall: 0.9091
          pass_rate: 0.9091
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 3827
        latency_ms: 3827
        cost_usd: 0.0188
        cost_estimated: true
        measured: 2026-06-30
        git_sha: fb2d00b
        note: >
          Bounded Anthropic challenger after the 2026-06-29 Claude Sonnet 5 API
          release. `python scripts/discover-models.py --check-new` confirmed
          `claude-sonnet-5` is live through the repo-local Anthropic key, so the
          run reused the direct Anthropic/no-sampling provider with adaptive
          thinking enabled, `output_config` effort high, and Sonnet-specific
          introductory token prices supplied by environment variables. Command:
          `cd benchmarks && ANTHROPIC_OPUS48_MODEL=claude-sonnet-5
          ANTHROPIC_MESSAGES_INPUT_PRICE_PER_1M=2
          ANTHROPIC_MESSAGES_OUTPUT_PRICE_PER_1M=10
          ../scripts/run_with_doc_web_env.py promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --providers
          python:$(pwd)/providers/anthropic_opus48_messages.py --output
          results/sonnet5-crop-page-level-deletion-gate-20260630.json
          --no-cache -j 1`. The run completed `20/22` with `0` provider
          errors, `overall = 0.9091`, average latency about `3827 ms`,
          `197513` prompt tokens, `1757` completion tokens, and estimated
          Sonnet 5 cost about `$0.4126` total (`$0.0188` per case). The misses
          were model-wrong false negatives on `page-122-000`, where the crop
          includes printed caption text below the family reunion photo, and
          `page-122-001`, where the crop leaks a neighboring portrait. An
          `xhigh` failed-case retry repaired `page-122-000` but still missed
          `page-122-001`, so no full xhigh rerun is warranted. This does not
          replace the maintained `openai:responses:gpt-5.5` page-context
          winner. Portable tracked proof:
          `docs/evals/attempts/014-sonnet5-bounded-challenger.md`.
      - model: "GPT-5.6 Sol Responses (page-context prompt, reasoning none)"
        metrics:
          overall: 0.8182
          pass_rate: 0.8182
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4743
        latency_ms: 4743
        cost_usd: 0.0644
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Bounded GPT-5.6 Sol page-context challenger through
          `benchmarks/providers/openai_responses_model.py` after official docs
          and model discovery confirmed the GPT-5.6 tiers. Result artifact:
          `benchmarks/results/gpt56-sol-crop-page-level-deletion-gate-20260711.json`.
          The run completed `18/22` with `0` provider errors, `overall =
          0.8182`, average latency about `4743 ms`, `276946` prompt tokens,
          `1033` completion tokens, and estimated Sol cost about `$1.4157` total
          (`$0.0644` per case). Failures were `page-001-000`,
          `page-122-001`, `page-125-000`, and `page-126-000`; manual image
          inspection classified them as model-wrong over-rejections of clean
          crops plus false negatives on the checked neighboring-portrait and
          page-text leakage cases. This does not replace the maintained
          `openai:responses:gpt-5.5` page-context winner. Portable tracked
          proof: `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.6 Terra Responses (page-context prompt, reasoning none)"
        metrics:
          overall: 0.9545
          pass_rate: 0.9545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 3963
        latency_ms: 3963
        cost_usd: 0.0322
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Bounded GPT-5.6 Terra page-context challenger through
          `benchmarks/providers/openai_responses_model.py` with reasoning effort
          `none`. Result artifact:
          `benchmarks/results/gpt56-terra-crop-page-level-deletion-gate-20260711.json`.
          The run completed `21/22` with `0` provider errors, `overall =
          0.9545`, average latency about `3963 ms`, `276946` prompt tokens,
          `1024` completion tokens, and estimated Terra cost about `$0.7077`
          total (`$0.0322` per case). The single miss was model-wrong on
          `page-122-001`: Terra returned `pass` for a crop that manual
          page-context inspection confirms leaks a neighboring Sophie L'Heureux
          portrait slice. Terra is cheaper and faster than the maintained GPT-5.5
          Responses gate in this pass, but quality is decisive and it does not
          preserve the `22/22` contract. Portable tracked proof:
          `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.6 Luna Responses (page-context prompt, reasoning none)"
        metrics:
          overall: 0.8636
          pass_rate: 0.8636
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4225
        latency_ms: 4225
        cost_usd: 0.0129
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Bounded GPT-5.6 Luna page-context challenger through
          `benchmarks/providers/openai_responses_model.py` with reasoning effort
          `none`. Result artifact:
          `benchmarks/results/gpt56-luna-crop-page-level-deletion-gate-20260711.json`.
          The run completed `19/22` with `0` provider errors, `overall =
          0.8636`, average latency about `4225 ms`, `276946` prompt tokens,
          `1187` completion tokens, and estimated Luna cost about `$0.2841`
          total (`$0.0129` per case). Failures were `page-001-000`,
          `page-021-001`, and `page-122-001`; manual image inspection classified
          these as model-wrong over-rejections of clean pass crops plus a false
          negative on neighboring-portrait leakage. This does not replace the
          maintained page-context winner. Portable tracked proof:
          `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.6 Terra Responses (page-context prompt, reasoning low)"
        metrics:
          overall: 0.9545
          pass_rate: 0.9545
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4928
        latency_ms: 4928
        cost_usd: 0.0330
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Full low-effort GPT-5.6 Terra page-context rerun after a targeted
          low-effort retry repaired the original `page-122-001` miss. Targeted
          artifact:
          `benchmarks/results/gpt56-terra-low-crop-page-level-targeted-20260711.json`;
          full rerun artifact:
          `benchmarks/results/gpt56-terra-low-crop-page-level-deletion-gate-20260711.json`.
          The full run completed `21/22` with `0` provider errors, `overall =
          0.9545`, average latency about `4928 ms`, `276946` prompt tokens,
          `2218` completion tokens, and estimated Terra cost about `$0.7256`
          total (`$0.0330` per case). Low effort fixed `page-122-001` in the
          full run but regressed to a model-wrong false negative on
          `page-126-000`, where manual inspection confirms visible adjacent
          page/plaque text. This does not replace the maintained page-context
          winner. Portable tracked proof:
          `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.6 Terra Responses (page-context prompt, reasoning medium)"
        metrics:
          overall: 0.9091
          pass_rate: 0.9091
          cases_total: 22
          pass_labeled: 17
          fail_labeled: 5
          latency_ms: 4945
        latency_ms: 4945
        cost_usd: 0.0334
        cost_estimated: true
        measured: 2026-07-11
        git_sha: 5efaa80
        note: >
          Full medium-effort GPT-5.6 Terra page-context rerun through
          `benchmarks/providers/openai_responses_model.py`. Result artifact:
          `benchmarks/results/gpt56-terra-medium-crop-page-level-deletion-gate-20260711.json`.
          The run completed `20/22` with `0` provider errors, `overall =
          0.9091`, average latency about `4945 ms`, `276946` prompt tokens,
          `2793` completion tokens, and estimated Terra cost about `$0.7343`
          total (`$0.0334` per case). Failures were model-wrong on
          `page-125-000` and `page-126-000`; effort was not monotonic and
          medium performed worse than reasoning `none` on this gate, so no
          higher-effort full rerun is warranted. Portable tracked proof:
          `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - model: "GPT-5.5 Pro Responses (page-context prompt)"
        metrics:
          overall: 0.6818
          pass_rate: 0.6818
          cases_total: 22
          pass_labeled: 18
          fail_labeled: 4
          latency_ms: 31803
        latency_ms: 31803
        cost_usd: 0.4269
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          GPT-5.5 Pro Responses rerun via
          `cd benchmarks && PROMPTFOO_EVAL_TIMEOUT_MS=180000 promptfoo eval -c
          tasks/crop-page-level-deletion-gate.yaml --no-cache
          --filter-providers '^openai:responses:gpt-5\.5-pro$' --output
          results/gpt55pro-responses-crop-page-level-deletion-gate-20260424.json
          -j 1`. The run completed with `15/22`, `0` provider errors, average
          latency about `31803 ms`, and estimated current API cost about
          `$9.3913` total (`$0.4269` per case). Failures were mostly
          over-rejections of golden pass crops plus empty/parse-fail outputs on
          `page-021-000`, `page-125-000`, and `page-126-000`. Portable tracked
          proof: `docs/evals/attempts/006-gpt55-responses-eval-refresh.md`.

    attempts:
      - id: "001"
        date: 2026-04-11
        status: partial
        approach: "Story 209 first draft of the page-context prompt on the new 22-case overlap corpus"
        score_before: null
        score_after: 0.9545
        note: >
          The first full run on the new page-context surface completed `21/22`
          with a single false negative on `page-126-000`. Classification:
          prompt-wrong, not golden-wrong. The prompt tolerated “minimal”
          adjacent plaque-text leakage even though the golden correctly labels
          that surrounding page text as a fail.
      - id: "002"
        date: 2026-04-11
        status: succeeded
        approach: "Tighten the page-context prompt to fail surrounding neighboring-visual text leakage, then rerun the page-level C5 gate"
        score_before: 0.9545
        score_after: 1.0
        note: >
          Tightening `benchmarks/prompts/validate-page-level-crop.js` to fail
          surrounding page text from adjacent plaques or neighboring figures
          removed the only false negative. The final rerun reached `22/22`
          correct labels and left the decision unchanged: C5 residue is still
          required because the checked corpus then contained `4` explicit fail
          cases in the maintained runtime.
      - id: "003"
        date: 2026-04-24
        status: failed
        approach: "New-subject-model screen: add callable GPT-5.5 to the page-context C5 deletion gate"
        score_before: 1.0
        score_after: 0.7273
        note: >
          GPT-5.5 did not preserve the maintained page-context validator
          contract. It over-rejected several golden pass crops as incomplete
          (`page-004-000`, `page-012-001`, `page-021-000`, `page-021-001`,
          `page-122-001`) and still missed the critical external-text failure
          on `page-126-000`. Manual inspection confirmed `page-004-000` is a
          tight but clean wagon crop, while `page-126-000` really includes
          external text along the left edge. No prompt, scorer, or golden
          changes were made.
      - id: "004"
        date: 2026-04-24
        status: failed
        approach: "Re-run the page-context C5 gate through OpenAI Responses for GPT-5.5 and GPT-5.5 Pro"
        score_before: 0.7273
        score_after: 0.9091
        note: >
          GPT-5.5 Responses improved over Chat (`20/22` vs `16/22`) but still
          missed the maintained `1.0` gate with false positives on
          `page-021-001` and then-golden-pass `page-122-001`; subsequent
          manual page-context inspection reclassified `page-122-001` as a
          golden error. GPT-5.5 Pro Responses scored only `15/22`, with several
          false positives and three empty outputs that the scorer could not
          parse.
      - id: "005"
        date: 2026-04-24
        status: succeeded
        approach: "Repair the GPT-5.5 Responses page-context prompt and correct one page-context golden"
        score_before: 0.9091
        score_after: 1.0
        note: >
          Manual inspection classified `page-021-001` as prompt-wrong: the
          large pale area is sky/snow/photo background inside the ranch image,
          not blank page margin. `page-122-001` was reclassified as
          golden-wrong for the page-context gate: the crop includes a visible
          slice of the neighboring Sophie L'Heureux portrait. The prompt now
          prioritizes external page text and neighboring-visual leakage,
          counts natural light image regions as image content, and only fails
          incompleteness when the full page clearly shows a large missing part
          of the intended visual. The final GPT-5.5 Responses rerun reached
          `22/22` on
          `benchmarks/results/gpt55-responses-crop-page-level-deletion-gate-promptfix-final-20260424.json`.
          An initial Gemini regression rerun was attempted at
          `benchmarks/results/g31-crop-page-level-deletion-gate-promptfix-20260424.json`
          but produced `22` provider errors because the configured Google API
          key was rejected. After switching promptfoo child processes to
          `DOC_WEB_GEMINI_API_KEY`, the fresh corrected-golden Gemini rerun
          completed with real model judgments but scored `21/22`, leaving
          GPT-5.5 Responses as the only current `22/22` page-context validator
          on the corrected golden.
      - id: "006"
        date: 2026-04-24
        status: failed
        approach: "Rerun Gemini 3.1 Flash Lite after DOC_WEB_GEMINI_API_KEY repair on the corrected page-context golden"
        score_before: 1.0
        score_after: 0.9545
        note: >
          The key-scope repair resolved the previous provider block: promptfoo
          produced `21/22` real judgments with `0` provider errors at
          `benchmarks/results/g31-crop-page-level-deletion-gate-promptfix-rerun-20260424.json`.
          The single remaining mismatch is model-wrong: Gemini returned
          `{"verdict": "pass"}` for `page-122-001` because it focused on
          excluding caption text and missed the visible neighboring Sophie
          L'Heureux portrait slice at the crop edge. The previous `1.0` Gemini
          score is now stale because it was measured before the `page-122-001`
          golden correction.
      - id: "007"
        date: 2026-04-24
        status: succeeded
        approach: "Apply GPT-5.5 Responses as the maintained page-context validator and rerun the narrowed task"
        score_before: 1.0
        score_after: 1.0
        note: >
          The maintained `benchmarks/tasks/crop-page-level-deletion-gate.yaml`
          provider list now contains only `openai:responses:gpt-5.5`, with the
          Gemini and Pro challenger outcomes preserved as recorded historical
          evidence instead of default providers. The applied rerun at
          `benchmarks/results/crop-page-level-deletion-gate-gpt55-responses-current-20260424.json`
          completed `22/22` with `0` failures and `0` provider errors. A narrow
          substrate test now enforces that this task defaults to the current
          corrected-golden winner.
      - id: "008"
        date: 2026-05-06
        status: failed
        approach: "Screen OpenAI `chat-latest` / GPT-5.5 Instant as a bounded page-context challenger after the 2026-05-05 release"
        score_before: 1.0
        score_after: 0.9545
        note: >
          `chat-latest` is image-callable through the local Responses shim, but
          it did not preserve the maintained page-context deletion-gate
          contract. The run scored `21/22` with `0` provider errors, average
          latency about `5698 ms`, and estimated cost about `$0.4136` total.
          The miss was model-wrong on `page-122-001`: it passed a crop that the
          corrected golden fails for leaking a neighboring portrait slice. No
          maintained providers, prompts, scorers, or goldens were changed.
      - id: "009"
        date: 2026-05-20
        status: failed
        approach: "Screen Kimi K2.6 through Moonshot's direct API as a bounded page-context challenger"
        score_before: 1.0
        score_after: 0.9545
        note: >
          Kimi K2.6 is callable on the two-image page-context prompt and is
          materially cheaper than the maintained GPT-5.5 Responses gate, but it
          did not preserve the required `22/22` quality contract. The run
          scored `21/22` with `0` provider errors, average latency about
          `9868 ms`, and estimated cost about `$0.1646` total. The miss was
          model-wrong on `page-122-001`: Kimi returned `pass` for a crop that
          the corrected golden fails for a visible neighboring portrait slice.
          Thinking mode did not repair the failed case. No maintained
          providers, prompts, scorers, or goldens were changed.
      - id: "010"
        date: 2026-05-20
        status: failed
        approach: "Screen Gemini 3.5 Flash as a bounded page-context validator challenger after paid-tier availability recovered"
        score_before: 1.0
        score_after: 0.954545
        note: >
          Initial promptfoo and direct REST attempts were blocked first by
          free-tier project quota and then by repeated `503 UNAVAILABLE`
          high-demand responses. After the heartbeat retry health probe
          succeeded on the canonical doc-web key with `serviceTier: standard`, I
          reran the 22-case page-context deletion gate through the direct REST
          fallback. The run scored `21/22` with `0` provider errors and missed
          `page-122-001`, where Gemini 3.5 Flash returned `pass` for a crop that
          the corrected golden fails for leaking a neighboring portrait slice.
          No maintained providers, prompts, scorers, or goldens were changed.
      - id: "011"
        date: 2026-05-28
        status: failed
        approach: "Screen Claude Opus 4.8 as a bounded page-context validator challenger after the Anthropic release"
        score_before: 1.0
        score_after: 0.9091
        note: >
          Claude Opus 4.8 is callable on the two-image page-context prompt once
          sampling params are omitted and adaptive thinking/high effort are used,
          but it did not preserve the maintained `22/22` quality contract. The
          run scored `20/22` with `0` provider errors, average latency about
          `4853 ms`, and estimated cost about `$1.0405` total. The misses were
          false negatives on the two corrected fail-labeled residue cases
          `page-122-001` and `page-126-000`; `xhigh` effort repaired `0/2`.
          No maintained providers, prompts, scorers, or goldens were changed.
      - id: "012"
        date: 2026-06-09
        status: failed
        approach: "Screen Claude Fable 5 as a bounded page-context validator challenger after the Anthropic release"
        score_before: 1.0
        score_after: 0.9091
        note: >
          Claude Fable 5 is callable on the two-image page-context prompt using
          the same direct Anthropic/no-sampling provider pattern as Opus 4.8,
          with Fable pricing recorded in the result artifact, but it did not
          preserve the maintained `22/22` quality contract. The run scored
          `20/22` with `0` provider errors, average latency about `7423 ms`,
          and estimated cost about `$2.1137` total. The misses were
          model-wrong false negatives on `page-122-001` and `page-126-000`.
          Manual image inspection confirmed both golden failures. Because Fable
          also loses latency and cost to the maintained GPT-5.5 Responses
          winner, no higher-effort retry is warranted. No maintained providers,
          prompts, scorers, or goldens were changed.
      - id: "013"
        date: 2026-06-30
        status: failed
        approach: "Screen Claude Sonnet 5 as a bounded page-context validator challenger after the Anthropic release"
        score_before: 1.0
        score_after: 0.9091
        note: >
          Claude Sonnet 5 is callable on the two-image page-context prompt using
          the same direct Anthropic/no-sampling provider pattern as Opus 4.8 and
          Fable 5, with Sonnet pricing recorded in the result artifact, but it
          did not preserve the maintained `22/22` quality contract. The high
          effort run scored `20/22` with `0` provider errors, average latency
          about `3827 ms`, and estimated cost about `$0.4126` total. The misses
          were model-wrong false negatives on `page-122-000` and
          `page-122-001`; manual image inspection confirmed both checked-in
          fail labels. An `xhigh` failed-case retry repaired `page-122-000` but
          still missed the neighboring-portrait residue on `page-122-001`, so no
          full xhigh rerun is warranted. Sonnet 5 is cheaper and faster than the
          maintained GPT-5.5 Responses winner on this surface, but quality is
          decisive here. No maintained providers, prompts, scorers, or goldens
          were changed.
      - id: "014"
        date: 2026-07-11
        status: failed
        approach: "Screen GPT-5.6 Sol, Terra, and Luna as bounded page-context validator challengers after the OpenAI release"
        score_before: 1.0
        score_after: 0.9545
        note: >
          Official OpenAI docs and `python scripts/discover-models.py
          --check-new` confirmed the GPT-5.6 Sol/Terra/Luna tiers. The sweep
          used `benchmarks/providers/openai_responses_model.py` because
          promptfoo 0.121.1's built-in OpenAI Responses provider sent
          unsupported chat-style image content blocks. The best full GPT-5.6
          page-context result was Terra reasoning `none` and Terra reasoning
          `low`, both `21/22` with `0` provider errors; `low` fixed the original
          `page-122-001` false negative but regressed to `page-126-000`.
          `medium` scored only `20/22`, Sol scored `18/22`, and Luna scored
          `19/22`. Manual contact-sheet inspection confirmed the failures were
          model-wrong, including false negatives on checked neighboring-portrait
          or page-text leakage cases and false positives on clean pass crops. No
          maintained providers, prompts, scorers, or goldens were changed.
          Portable tracked proof:
          `docs/evals/attempts/015-gpt56-tier-sweep.md`.
      - id: "015"
        date: 2026-07-20
        status: failed
        approach: "Gate Grok 4.5 page-context evaluation behind the maintained detector prerequisite"
        score_before: 1.0
        score_after: null
        note: >
          The companion Grok 4.5 detector run was transport-clean but scored
          only `0.8026` versus the `0.95` target and maintained `0.9703` winner.
          A high-reasoning retry repeated its model-wrong `Image011` bbox
          undercoverage. The 22-case page-context gate was intentionally not
          started because the candidate failed the cheaper prerequisite and
          could not qualify for adoption. Retry only after a materially revised
          Grok model or new visual-grounding evidence. Portable proof:
          `docs/evals/attempts/016-grok45-bounded-challenger.md`.
      - id: "016"
        date: 2026-07-21
        status: failed
        approach: "Promote Gemini 3.6 Flash to the page-context gate after its clean detector transport"
        score_before: 1.0
        score_after: 0.9545
        note: >
          Gemini 3.6 Flash completed `21/22` and falsely passed
          `page-122-001`; manual inspection confirmed neighboring-portrait
          leakage. Flash-Lite was not promoted after its losing detector and
          crop-only results. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.

    retry_when:
      - condition: new-subject-model
        note: >
          Rerun when a materially stronger validator or detector may reduce the
          current fail-labeled page-context cases.
      - condition: runtime-change
        note: >
          Rerun after any maintained crop/runtime simplification before claiming
          `trim_layout_text` or bounded caption assist can be deleted.
      - condition: golden-fix
        note: >
          Rerun if the page-context overlap corpus or fail reasons change.

  # ── Quality Evals (OCR) ──────────────────────────────────────────────────

  - id: ocr-model-genealogy
    name: OCR Model Eval — Genealogy / Table-Heavy Books
    type: quality
    description: >
      Evaluate OCR quality for table-heavy layouts (Onward to the Unknown).
      Tests text accuracy, table structure preservation, and column alignment.
    pipeline_stage: ocr
    runner: manual
    story_refs: ["127", "140"]
    category_refs: ["spec:2", "spec:3"]
    compromise_refs: ["C1"]
    spec_compromise: C1

    target:
      metric: character_accuracy
      value: 0.99

    scores:
      - model: "GPT-5.1 (AI-first OCR)"
        metrics:
          character_accuracy: 0.97
        measured: 2026-02-20
        note: "Story 127 — good for prose, struggles with dense tables"

    attempts:
      - id: "001"
        date: 2026-02-20
        status: partial
        approach: "Benchmarked GPT-5.1 on Onward table pages"
        note: "Prose excellent; tables need rescue pass or layout-aware model"
      - id: "002"
        date: 2026-03-14
        status: succeeded
        approach: "Story 140 page-golden coverage expansion for reviewed Roseanna/Emilie continuations"
        note: >
          Added hand-verified page goldens `page-089`, `page-097`, and `page-098`
          to `benchmarks/tasks/ocr-genealogy-tables.yaml` and
          `benchmarks/tasks/ocr-genealogy-tables-gpt-only.yaml`. Story 140
          validated those pages by direct artifact-vs-golden scoring on
          `story140-onward-targeted-rescue-r7`, `r13`, and `r19` rather than a
          fresh promptfoo model sweep, so no new comparable model score is
          recorded here yet.
      - id: "003"
        date: 2026-04-24
        status: blocked
        approach: "Add GPT-5.5 to the GPT-only promptfoo screen and attempt a table-strict rerun"
        score_before: null
        score_after: null
        note: >
          Added `openai:chat:gpt-5.5` to
          `benchmarks/tasks/ocr-genealogy-tables-gpt-only.yaml`, then attempted
          `cd benchmarks && promptfoo eval -c
          tasks/ocr-genealogy-tables-gpt-only.yaml --no-cache
          --filter-providers 'openai:chat:gpt-5.5' --filter-prompts
          'table-strict' --output
          results/gpt55-ocr-genealogy-tables-table-strict-20260424.json -j 1`.
          The first run aborted before any provider call because
          `benchmarks/input/onward-pages-b64/Image007.b64.txt` is missing.
          This is the broader benchmark-substrate drift already documented in
          Story 183. A temporary local full-resolution raw-image base64 attempt
          was stopped and removed because those `5100x6600` page encodings are
          not the original downscaled canonical fixtures and would make the
          score non-comparable.

    retry_when:
      - condition: new-subject-model
        note: "Models with native table understanding may eliminate rescue pass"
      - condition: benchmark-substrate-restored
        note: >
          Rerun when the canonical `benchmarks/input/onward-pages-b64/`
          fixtures are restored or regenerated with documented dimensions.

  - id: handwritten-notes-transcription
    name: Handwritten Notes Transcription
    type: quality
    description: >
      Repo-owned handwritten-note transcription fidelity on the bounded
      maintained handwritten rescue image-directory and PDF OCR lanes.
      Measures normalized text fidelity against the checked-in transcripts for
      the current handwritten corpus: the highly legible mini fixture from
      Story 179, the faded synthetic variant from Story 185, the rougher
      synthetic variant from Story 182, the first real LOC Barney spread from
      Story 186, and the second real LOC Alverson front page added during
      Story 189 close-out and corrected to the visible front-page boundary in
      Story 192.
    pipeline_stage: ocr
    runner: manual
    command: "python benchmarks/scripts/run_handwritten_notes_eval.py --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --instrument --output benchmarks/results/handwritten-notes-story189-real-expanded.json"
    scorer: benchmarks/scorers/handwritten_notes_transcription.py
    golden: benchmarks/golden/handwritten-notes/corpus.json
    story_refs: ["179", "182", "185", "186", "189", "191", "192", "208", "230"]
    category_refs: ["spec:1", "spec:2", "spec:8"]
    compromise_refs: []

    target:
      metric: overall_min_ratio
      value: 0.99
      note: "The target still marks the desired passing bar, but the widened default corpus now includes two real handwritten fixtures that the maintained rescue seam does not clear."

    scores:
      - model: "Gemini 3.6 Flash bounded corrected-real image-entry screen"
        metrics:
          overall_min_ratio: 0.984052
          page_min_ratio: 0.984052
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 2
          fixtures_passing: 0
          barney_image_overall_ratio: 0.984527
          alverson_image_overall_ratio: 0.984052
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Real `driver.py` image-entry artifacts were manually checked against
          both LOC scans. The corrected scorer disables SequenceMatcher
          autojunk; remaining literal-transcription differences are genuine
          model errors, and neither fixture clears `0.99`. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "Gemini 3.5 Flash-Lite bounded corrected-real image-entry screen"
        metrics:
          overall_min_ratio: 0.974964
          page_min_ratio: 0.974964
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 2
          fixtures_passing: 0
          barney_image_overall_ratio: 0.974964
          alverson_image_overall_ratio: 0.989935
        measured: 2026-07-21
        git_sha: ac574bf
        note: >
          Real `driver.py` image-entry artifacts were manually checked against
          both LOC scans. Output was plausible but preserved neither real
          transcript at the strict `0.99` bar; Story 191 remains blocked.
          Portable proof: `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.
      - model: "Unlimited-OCR exact-weight FP32 CPU bounded real-fixture screen"
        metrics:
          overall_min_ratio: 0.022946
          page_min_ratio: 0.022946
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 2
          fixtures_passing: 0
          barney_image_overall_ratio: 0.200382
          alverson_image_overall_ratio: 0.022946
          barney_latency_ms: 5388
          alverson_latency_ms: 764699
        measured: 2026-07-20
        git_sha: ac574bf
        note: >
          Story 230 ran the pinned exact Unlimited-OCR weight object through a
          narrowly reviewed device/dtype-only community runtime on the corrected
          Barney and Alverson real image fixtures. The candidate reached only
          `0.200382` on Barney and `0.022946` on Alverson, with `0/2` cases at
          the `0.99` bar. Barney omitted substantial visible text. Alverson
          emitted `30939` tokens against total `max_length = 32768`, hallucinated
          math, and repeated table-like sequences. Because the helper omitted
          input length and stop reason, truncation remains indeterminate; the
          runaway content is independently a decisive quality failure. The
          public-sample parity control matched Baidu's
          official BF16 Space exactly after removal of the Space's grounding
          metadata, so the large result deficit is not close enough to justify
          an official CUDA rerun. Story 191 remains blocked. Portable proof:
          `docs/evals/attempts/017-unlimited-ocr-whole-document-challenger.md`.
      - model: "Maintained handwritten rescue OCR lanes (gemini-2.5-pro via handwritten-notes rescue recipes)"
        metrics:
          overall_min_ratio: 0.677267
          page_min_ratio: 0.677267
          pass_rate: 0.6
          cases_total: 10
          fixture_count: 5
          fixtures_passing: 3
        measured: 2026-04-04
        note: >
          Story 192 corrected the Alverson transcript to the exact visible
          front-page boundary of the checked-in LOC source image and reran the
          maintained rescue seam via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --output benchmarks/results/handwritten-notes-story192-alverson-frontpage.json`,
          which wrote
          `benchmarks/results/handwritten-notes-story192-alverson-frontpage.json`
          with `pass_rate = 0.6`, `overall_min_ratio = 0.677267`,
          `page_min_ratio = 0.677267`, `cases_total = 10`, and
          `fixtures_passing = 3`. The six synthetic rescue cases still scored
          `1.0`; Barney remained below bar at `0.908567` on image entry and
          `0.886568` on PDF entry at
          `output/runs/handwritten-handwritten-notes-barney-real-image-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-barney-real-pdf-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`;
          the corrected Alverson front-page fixture now scores `0.677267` on
          image entry and `0.681952` on PDF entry at
          `output/runs/handwritten-handwritten-notes-alverson-real-image-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-alverson-real-pdf-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Manual artifact inspection in the same pass confirmed the prior
          `0.499136` floor was partially golden-wrong rather than a pure OCR
          miss: the transcript/source mismatch is repaired, but the maintained
          rescue seam still misreads visible Alverson text (`nothing`,
          `Battle of Chickamauga`, `lates novels`, `excepatable`) and still
          drifts semantically on Barney. The handwritten family therefore
          remains `has-fixture`, not `passing`.
      - model: "Gemini 3.5 Flash bounded real-fixture image-entry screen"
        metrics:
          overall_min_ratio: 0.24735
          page_min_ratio: 0.24735
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 2
          fixtures_passing: 0
          barney_image_overall_ratio: 0.807498
          alverson_image_overall_ratio: 0.24735
        measured: 2026-05-20
        git_sha: 99450a3
        note: >
          Bounded Gemini 3.5 Flash handwritten OCR screen after the canonical
          doc-web key recovered from free-tier quota and provider high-demand
          blockers. The image-entry-only screen used the corrected Barney and
          Alverson real fixtures without editing maintained recipes. Result
          summary:
          `benchmarks/results/handwritten-notes-gemini35-flash-image-screen-20260520.json`.
          OCR artifacts:
          `output/runs/eval-barney-image-gemini-3-5-flash/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/eval-alverson-image-gemini-3-5-flash/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Barney reached `0.807498`, but Alverson reached only `0.247350`, so
          the screen floor was `0.247350` with `0/2` cases clearing the `0.99`
          bar. Manual inspection confirmed the low Alverson score reflects
          wrong-source text such as `Danville Va Nov 18th 1863` and `Battle of
          Chickamauga`, not a scoring quirk. Because Gemini 3.5 Flash did not
          beat both corrected real fixtures, it was not promoted to a PDF-entry
          or full five-fixture rerun.
      - model: "Claude Opus 4.8 bounded real-fixture image-entry screen"
        metrics:
          overall_min_ratio: 0.680902
          page_min_ratio: 0.680902
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 2
          fixtures_passing: 0
          barney_image_overall_ratio: 0.711207
          alverson_image_overall_ratio: 0.680902
          cost_usd: 0.0215
          cost_estimated: true
        measured: 2026-05-28
        git_sha: 5254df1
        note: >
          Bounded Claude Opus 4.8 handwritten OCR screen after the Anthropic
          release and the Opus 4.8 sampling-parameter compatibility fix in
          `modules/common/anthropic_client.py`. The image-entry-only screen used
          the corrected Barney and Alverson real fixtures without editing
          maintained recipes. OCR artifacts:
          `output/runs/eval-barney-image-opus-4-8/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/eval-alverson-image-opus-4-8/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Barney reached `0.711207` and Alverson reached `0.680902`, so the
          screen floor was `0.680902` with `0/2` cases clearing the `0.99` bar.
          Instrumentation recorded `4027` prompt tokens, `918` completion
          tokens, and estimated Opus 4.8 cost about `$0.0431` total (`$0.0215`
          per case). The candidate barely beat corrected Alverson image-entry
          but regressed materially from maintained Barney evidence, so it was
          not promoted to PDF-entry or full-corpus reruns. Portable tracked
          proof: `docs/evals/attempts/012-opus48-bounded-challenger.md`.
      - model: "Maintained handwritten rescue OCR lanes (gemini-2.5-pro via handwritten-notes rescue recipes)"
        metrics:
          overall_min_ratio: 0.499136
          page_min_ratio: 0.499136
          pass_rate: 0.6
          cases_total: 10
          fixture_count: 5
          fixtures_passing: 3
          latency_ms: 17388.9
          cost_usd: 0.005563
          cost_estimated: true
        measured: 2026-04-04
        note: >
          Story 191 reran the maintained rescue seam on the current tip via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --output benchmarks/results/handwritten-notes-story191-baseline-20260404.json`,
          which wrote
          `benchmarks/results/handwritten-notes-story191-baseline-20260404.json`
          with `pass_rate = 0.6`, `overall_min_ratio = 0.499136`,
          `page_min_ratio = 0.499136`, `cases_total = 10`, and
          `fixtures_passing = 3`. The six synthetic rescue cases still scored
          `1.0`, but the real fixtures kept the family below bar: Barney
          reached `0.895587` on image entry and `0.756036` on PDF entry at
          `output/runs/handwritten-handwritten-notes-barney-real-image-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-barney-real-pdf-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          while Alverson reached `0.499136` on image entry and `0.500647` on
          PDF entry at
          `output/runs/handwritten-handwritten-notes-alverson-real-image-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-alverson-real-pdf-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Story 191 then measured fresh direct `gpt-5` and
          `claude-opus-4-6` probes and still found no winner. Manual source
          review in the same pass also changed the Alverson interpretation:
          the checked-in front-page image itself visibly ends at the same
          `...exceptable and` boundary where OCR stops, while the LOC source
          record exposes separate front/back images. That makes the current
          Alverson truth surface at least partially a source/transcript
          alignment problem rather than a pure OCR runtime miss. The
          handwritten family therefore stays `has-fixture`, not `passing`.
          Story 192 later corrected that transcript boundary and remeasured
          the corpus, so this entry is historical pre-fix evidence rather than
          the current baseline.
      - model: "Maintained handwritten rescue OCR lanes (gemini-2.5-pro via handwritten-notes rescue recipes)"
        metrics:
          overall_min_ratio: 0.883604
          page_min_ratio: 0.883604
          pass_rate: 0.75
          cases_total: 8
          fixture_count: 4
          fixtures_passing: 3
          latency_ms: 18366.581
          cost_usd: 0.006059
          cost_estimated: true
        measured: 2026-04-04
        note: >
          Story 189 landed the bounded maintained handwritten rescue recipes
          `configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml`
          and
          `configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml`,
          plus eval-helper instrumentation capture in
          `benchmarks/scripts/run_handwritten_notes_eval.py`. Fresh rerun via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --instrument --output benchmarks/results/handwritten-notes-story189-handwritten-rescue.json`
          wrote
          `benchmarks/results/handwritten-notes-story189-handwritten-rescue.json`
          with `pass_rate = 0.75`, `overall_min_ratio = 0.883604`,
          `page_min_ratio = 0.883604`, `cases_total = 8`, and
          `fixtures_passing = 3`. The six synthetic rescue cases still scored
          `1.0`, while the real Barney slice improved materially to
          `0.883604` on image entry and `0.884339` on PDF entry at
          `output/runs/handwritten-handwritten-notes-barney-real-image-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-barney-real-pdf-handwritten-rescue/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Instrumented OCR-stage averages were `18366.581 ms` and
          `$0.006059` per case from the run-local `instrumentation.json`
          files. Manual inspection confirmed the rescue path now preserves
          harder Barney phrases like `real sorrow`, `father had gone to see
          him`, and the second-paragraph tents / cedar-brush passage, while
          still drifting on names and words such as `Harlen`, `genesee`,
          `Welthy gone`, and `med of cloth`. The family therefore remains
          `has-fixture`, not `passing`.
      - model: "Maintained generic OCR lanes (single-fixture Barney current-tip rerun via recipe-images-ocr-html-mvp.yaml + recipe-pdf-ocr-html-mvp.yaml)"
        metrics:
          overall_min_ratio: 0.105378
          page_min_ratio: 0.105378
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 1
          fixtures_passing: 0
          latency_ms: 8058.037
          cost_usd: 0.00483
          cost_estimated: true
        measured: 2026-04-04
        note: >
          After OpenAI credit was restored, Story 189 reran the maintained
          generic Barney baseline on the current tip via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --transcript testdata/handwritten-notes-barney-real.txt --images testdata/handwritten-notes-barney-real-images --pdf testdata/handwritten-notes-barney-real.pdf --instrument --output benchmarks/results/story189-barney-generic-current.json`,
          which wrote `benchmarks/results/story189-barney-generic-current.json`
          with `pass_rate = 0.0`, `overall_min_ratio = 0.105378`, and
          `page_min_ratio = 0.105378`. The fresh generic image-entry rerun
          reached `0.111437` at
          `output/runs/handwritten-single-fixture-image-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and the fresh generic PDF-entry rerun reached `0.105378` at
          `output/runs/handwritten-single-fixture-pdf-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Instrumented OCR-stage averages were `8058.037 ms` and `$0.00483`
          per case from the run-local `instrumentation.json` files. Manual
          inspection confirmed the same dominant failure pattern as Story 186:
          semantically wrong rewrites like `the combat`, `old home town is all
          gone`, and `wife of Harlow`, which keeps the maintained generic seam
          far below the bounded rescue path.
      - model: "Bounded direct real-fixture comparison (gemini-2.5-pro on the Barney spread)"
        metrics:
          overall_min_ratio: 0.864029
          page_min_ratio: 0.864029
          pass_rate: 0.0
          cases_total: 2
          fixture_count: 1
        measured: 2026-04-04
        note: >
          Story 189 reran the bounded direct current-tip Barney comparison on
          the checked-in image and PDF manifests using `gemini-2.5-pro` plus
          handwritten-focused hints via
          `PYTHONPATH=. python modules/extract/ocr_ai_gpt51_v1/main.py ...`.
          The direct image-manifest result scored `0.864029` at
          `output/runs/story189-barney-direct-gemini-image/01_ocr_ai_gpt51_v1/pages_html.jsonl`
          and the direct PDF-manifest result scored `0.894151` at
          `output/runs/story189-barney-direct-gemini-pdf/01_ocr_ai_gpt51_v1/pages_html.jsonl`.
          This established Gemini as the strongest currently runnable
          single-call candidate before the bounded maintained rescue recipe was
          introduced.
      - model: "Maintained generic OCR lanes (four-fixture mixed corpus via recipe-images-ocr-html-mvp.yaml + recipe-pdf-ocr-html-mvp.yaml)"
        metrics:
          overall_min_ratio: 0.102602
          page_min_ratio: 0.102602
          pass_rate: 0.75
          cases_total: 8
          fixture_count: 4
          fixtures_passing: 3
        measured: 2026-04-03
        note: >
          Story 186 added the first repo-owned real handwritten fixture:
          `testdata/handwritten-notes-barney-real-images/`,
          `testdata/handwritten-notes-barney-real.pdf`, and
          `testdata/handwritten-notes-barney-real.txt`, derived from Library
          of Congress item `https://www.loc.gov/pictures/item/2023637825/`
          with source rights note `No known restrictions on publication.`.
          Fresh rerun via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --output benchmarks/results/handwritten-notes-story186-real-fixture.json`
          wrote `benchmarks/results/handwritten-notes-story186-real-fixture.json`;
          subsequent reruns stayed in the same low-0.1 range on the real
          Barney slice while preserving `pass_rate = 0.75` for the mixed
          four-fixture corpus. Image-only PDF verification stayed `[0]` for
          `handwritten-notes-barney-real` alongside the still-passing synthetic
          fixtures `[0, 0]`, `[0, 0]`, and `[0, 0, 0]`. Manual artifact
          inspection confirmed stamped manifest provenance at
          `output/runs/handwritten-handwritten-notes-barney-real-image-generic/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-barney-real-pdf-generic/01_extract_pdf_images_fast_v1/pages_images_manifest.jsonl`,
          while the OCR artifacts at
          `output/runs/handwritten-handwritten-notes-barney-real-image-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-barney-real-pdf-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          showed coherent-looking but semantically wrong substitutions such as
          `Wm H. Harlow` / `the combat`, so the widened family no longer
          qualifies as a blanket handwritten passing claim. Story 189 tried to
          rerun this maintained generic baseline on 2026-04-04, but the
          current checkout hit OpenAI `insufficient_quota` before OCR output,
          so this low-0.1 entry remains the freshest inspected generic-baseline
          reference while the quota blocker is active.
      - model: "Bounded direct real-fixture comparison (claude-opus-4-6 on the Barney spread)"
        metrics:
          overall_min_ratio: 0.725708
          page_min_ratio: 0.725708
          pass_rate: 0.0
          cases_total: 1
          fixture_count: 1
        measured: 2026-04-03
        note: >
          Per Story 186's comparison gate, a one-off non-maintained direct
          transcription rerun on the same real Barney spread used
          `PYTHONPATH=. python modules/extract/ocr_ai_gpt51_v1/main.py --pages output/runs/handwritten-handwritten-notes-barney-real-image-generic/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl --outdir output/runs/story186-barney-direct-claude-opus/01_ocr_ai_gpt51_v1 --out pages_html.jsonl --model claude-opus-4-6 ...`
          and then scored the result with
          `benchmarks/scorers/handwritten_notes_transcription.py`. The bounded
          comparison improved the real-slice score materially to roughly
          `0.73` at
          `output/runs/story186-barney-direct-claude-opus/01_ocr_ai_gpt51_v1/pages_html.jsonl`,
          preserving harder real details like `Alvira and Welington` and
          `real sorrow`, but it still missed the repo's `0.99` passing bar and
          therefore does not justify a maintained handwritten runtime change by
          itself.
      - model: "Maintained generic OCR lanes (recipe-images-ocr-html-mvp.yaml + recipe-pdf-ocr-html-mvp.yaml)"
        metrics:
          overall_min_ratio: 1.0
          page_min_ratio: 1.0
          pass_rate: 1.0
          cases_total: 6
          fixture_count: 3
        measured: 2026-04-03
        note: >
          Story 185 added `testdata/handwritten-notes-faded-images/`, the
          image-only wrapper `testdata/handwritten-notes-faded.pdf`, the
          transcript `testdata/handwritten-notes-faded.txt`, and widened the
          maintained corpus at
          `benchmarks/golden/handwritten-notes/corpus.json` to three fixtures.
          Fresh rerun via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --output benchmarks/results/handwritten-notes-story185-widened.json`
          wrote `benchmarks/results/handwritten-notes-story185-widened.json`
          with `pass_rate = 1.0`, `overall_min_ratio = 1.0`,
          `page_min_ratio = 1.0`, `cases_total = 6`, `fixture_count = 3`,
          and image-only PDF verification of `[0, 0]` for
          `handwritten-notes-mini`, `[0, 0]` for
          `handwritten-notes-faded`, and `[0, 0, 0]` for
          `handwritten-notes-rough`. Manual artifact inspection in the same
          pass confirmed stamped manifest provenance at
          `output/runs/handwritten-handwritten-notes-mini-image-generic/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`,
          `output/runs/handwritten-handwritten-notes-faded-image-generic/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`,
          `output/runs/handwritten-handwritten-notes-rough-image-generic/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`,
          `output/runs/handwritten-handwritten-notes-mini-pdf-generic/01_extract_pdf_images_fast_v1/pages_images_manifest.jsonl`,
          `output/runs/handwritten-handwritten-notes-faded-pdf-generic/01_extract_pdf_images_fast_v1/pages_images_manifest.jsonl`,
          and `output/runs/handwritten-handwritten-notes-rough-pdf-generic/01_extract_pdf_images_fast_v1/pages_images_manifest.jsonl`,
          plus normalized transcript matches in
          `output/runs/handwritten-handwritten-notes-faded-image-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/handwritten-handwritten-notes-faded-pdf-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          where page 2 preserved the harder snippet
          "The parcel for J. Mercer should include 3 seals, one wax cord, and the note from April 8."
          without loss on both maintained lanes.
      - model: "Maintained generic OCR lanes (recipe-images-ocr-html-mvp.yaml + recipe-pdf-ocr-html-mvp.yaml)"
        metrics:
          overall_min_ratio: 1.0
          page_min_ratio: 1.0
          pass_rate: 1.0
          cases_total: 4
          fixture_count: 2
        measured: 2026-04-03
        note: >
          Story 182 added `testdata/handwritten-notes-rough-images/`, the
          image-only wrapper `testdata/handwritten-notes-rough.pdf`, corpus
          metadata at `benchmarks/golden/handwritten-notes/corpus.json`, and
          widened `benchmarks/scripts/run_handwritten_notes_eval.py` to emit
          fixture-specific run IDs. Fresh rerun via that helper wrote
          `benchmarks/results/handwritten-notes-story182.json` with
          `pass_rate = 1.0`, `overall_min_ratio = 1.0`,
          `page_min_ratio = 1.0`, `cases_total = 4`, and image-only PDF
          verification of `[0, 0]` for `handwritten-notes-mini` plus
          `[0, 0, 0]` for `handwritten-notes-rough`. Manual artifact
          inspection in the same pass confirmed normalized transcript matches
          at
          `output/runs/handwritten-handwritten-notes-mini-image-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          `output/runs/handwritten-handwritten-notes-mini-pdf-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          `output/runs/handwritten-handwritten-notes-rough-image-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          and
          `output/runs/handwritten-handwritten-notes-rough-pdf-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
      - model: "Maintained generic OCR lanes (recipe-images-ocr-html-mvp.yaml + recipe-pdf-ocr-html-mvp.yaml)"
        metrics:
          overall_ratio: 1.0
          page_min_ratio: 1.0
          pass_rate: 1.0
          image_overall_ratio: 1.0
          pdf_overall_ratio: 1.0
        measured: 2026-04-01
        note: >
          Story 179 added `testdata/handwritten-notes-mini-images/`, the
          image-only wrapper `testdata/handwritten-notes-mini.pdf`, and the
          focused regression helper
          `benchmarks/scripts/run_handwritten_notes_eval.py`. Fresh rerun via
          that helper wrote
          `benchmarks/results/handwritten-notes-story179.json` with
          `pass_rate = 1.0`, `overall_min_ratio = 1.0`, and
          `page_min_ratio = 1.0`; the helper also verified the PDF wrapper
          stayed image-only (`[0, 0]` extract-text lengths). Manual artifact
          inspection in the same pass confirmed exact normalized transcript
          matches at
          `output/runs/story179-image-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/story179-pdf-generic/02_ocr_ai_gpt51_v1/pages_html.jsonl`.

    attempts:
      - id: "001"
        date: 2026-04-01
        status: succeeded
        approach: "Story 179 repo-owned synthetic handwritten fixture through the maintained generic image-directory and PDF OCR lanes"
        score_before: null
        score_after: 1.0
        note: >
          Added the first repo-owned handwritten-notes fixture and a focused
          scoring helper instead of a new runtime module. The checked-in
          synthetic render proved legible enough that the maintained generic OCR
          seam already cleared the narrow slice, so no handwritten-only recipe
          was introduced.
      - id: "002"
        date: 2026-04-03
        status: succeeded
        approach: "Story 182 widened the maintained handwritten proof surface to a two-fixture synthetic corpus through the maintained generic image-directory and PDF OCR lanes"
        score_before: 1.0
        score_after: 1.0
        note: >
          Added the rough synthetic handwritten fixture, corpus metadata, and
          fixture-specific run IDs instead of introducing a handwritten-only
          runtime seam. An initial rough probe with dash-ledger bullets reached
          only `page_min_ratio = 0.977612` because the dash markers were
          dropped; that was treated as an ambiguous fixture-design edge rather
          than proof that the maintained seam needed new logic, so the final
          rough fixture text was tightened to emphasize rough handwriting
          content instead of punctuation-only loss. The final four-case corpus
          rerun passed cleanly at `1.0`, so the maintained generic seam now has
          broader but still synthetic support.
      - id: "003"
        date: 2026-04-04
        status: succeeded
        approach: "Story 189 bounded handwritten rescue recipes with Gemini plus current-pass Barney decision-surface instrumentation"
        score_before: 0.102602
        score_after: 0.883604
        note: >
          First current-pass action was a fresh rerun attempt of the maintained
          generic Barney baseline, but
          `python benchmarks/scripts/run_handwritten_notes_eval.py --transcript testdata/handwritten-notes-barney-real.txt --images testdata/handwritten-notes-barney-real-images --pdf testdata/handwritten-notes-barney-real.pdf --output benchmarks/results/story189-barney-baseline.json`
          failed with OpenAI `insufficient_quota`, so the direct decision
          surface shifted to current-pass Gemini/Claude comparisons on the same
          Barney manifests. That evidence justified a bounded maintained rescue
          seam instead of deterministic handwriting heuristics or a generic OCR
          recipe change. Story 189 then added handwritten-specific Gemini
          rescue recipes plus helper instrumentation capture and reran the full
          four-fixture corpus, improving the maintained real-slice floor from
          `0.102602` to `0.883604` while preserving six synthetic cases at
          `1.0`. The rescue seam still misses the repo's `0.99` bar on Barney,
          so the family remains `has-fixture`.
      - id: "004"
        date: 2026-04-04
        status: succeeded
        approach: "Fresh current-tip maintained generic Barney rerun after OpenAI quota restoration"
        score_before: 0.102602
        score_after: 0.105378
        note: >
          Once additional OpenAI credit was available, Story 189 reran the
          maintained generic Barney baseline with instrumentation via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --transcript testdata/handwritten-notes-barney-real.txt --images testdata/handwritten-notes-barney-real-images --pdf testdata/handwritten-notes-barney-real.pdf --instrument --output benchmarks/results/story189-barney-generic-current.json`.
          The generic seam stayed in the same low-0.1 band on the current tip
          (`0.111437` image, `0.105378` pdf), so the bounded Gemini rescue
          decision was confirmed rather than overturned.
      - id: "005"
        date: 2026-04-04
        status: blocked
        approach: "Story 191 final handwritten-arc close attempt: rerun maintained baseline, compare fresh stronger direct-model probes, and inspect the LOC source assets"
        score_before: 0.496975
        score_after: 0.499136
        note: >
          Story 191 first reran the maintained rescue seam on the current tip
          via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --output benchmarks/results/handwritten-notes-story191-baseline-20260404.json`,
          which stayed below bar at `0.499136 / 0.499136 / 0.6`
          (overall_min / page_min / pass_rate). Fresh direct `gpt-5` probes at
          `output/runs/story191-barney-image-gpt5-direct/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          `output/runs/story191-barney-pdf-gpt5-direct/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          `output/runs/story191-alverson-image-gpt5-direct/02_ocr_ai_gpt51_v1/pages_html.jsonl`,
          and
          `output/runs/story191-alverson-pdf-gpt5-direct/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          were materially worse (`0.192010`, `0.010297`, `0.221929`,
          `0.362143`), and direct `claude-opus-4-6` image probes at
          `output/runs/story191-barney-image-claude-direct/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/story191-alverson-image-claude-direct/02_ocr_ai_gpt51_v1/pages_html.jsonl`
          also stayed below bar (`0.725708`, `0.193325`). Manual source review
          of `testdata/handwritten-notes-alverson-real-images/page-001.jpg`
          plus the LOC source record `https://www.loc.gov/pictures/item/2023637835/`
          showed that the checked-in front-page image itself ends at the same
          `...exceptable and` boundary where OCR stops and that the LOC item
          exposes separate front/back images. Result: Story 191 was blocked
          honestly instead of landing speculative OCR glue against a misaligned
          truth surface.
      - id: "006"
        date: 2026-04-04
        status: succeeded
        approach: "Story 192 corrected the Alverson transcript to the visible front-page boundary, reran the maintained handwritten corpus, and reclassified the prior floor as partially golden-wrong"
        score_before: 0.499136
        score_after: 0.677267
        note: >
          Story 192 first verified that the checked-in Alverson image is a
          single front page that visibly ends at `...exceptable and` while the
          LOC item `https://www.loc.gov/pictures/item/2023637835/` exposes a
          separate back page. It then trimmed
          `testdata/handwritten-notes-alverson-real.txt` to that visible
          boundary, added a focused scope test, reran a single-fixture probe,
          and finally reran the full maintained corpus via
          `benchmarks/results/handwritten-notes-story192-alverson-frontpage.json`.
          Verified result: the corpus floor improved from `0.499136` to
          `0.677267` without any OCR runtime change, which classifies the
          previous floor as partially **test-wrong / golden-wrong**. The
          remaining failure is now clearly **model-wrong** on the corrected
          real fixtures, especially Barney (`0.908567` / `0.886568`) and the
          visible Alverson front page (`0.677267` / `0.681952`).
      - id: "007"
        date: 2026-04-05
        status: failed
        approach: "Bounded new-subject-model screen on the corrected real-fixture pair after verifying current model availability"
        score_before: 0.677267
        score_after: 0.677267
        note: >
          Per `/improve-eval handwritten-notes-transcription`, I first
          verified current provider availability with
          `python scripts/discover-models.py --check-new`, which showed fresh
          callable frontier candidates including `gpt-5.4-pro` and the Gemini
          3.x family in this checkout with all three API keys present. I then
          reran the maintained handwritten rescue baseline on the corrected
          real fixtures via
          `python benchmarks/scripts/run_handwritten_notes_eval.py --transcript testdata/handwritten-notes-barney-real.txt --images testdata/handwritten-notes-barney-real-images --pdf testdata/handwritten-notes-barney-real.pdf --fixture-id handwritten-notes-barney-real --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --output benchmarks/results/handwritten-notes-improve-eval-20260405-barney-baseline.json`
          and
          `python benchmarks/scripts/run_handwritten_notes_eval.py --transcript testdata/handwritten-notes-alverson-real.txt --images testdata/handwritten-notes-alverson-real-images --pdf testdata/handwritten-notes-alverson-real.pdf --fixture-id handwritten-notes-alverson-real --image-recipe configs/recipes/recipe-images-ocr-html-handwritten-notes-gemini-rescue.yaml --pdf-recipe configs/recipes/recipe-pdf-ocr-html-handwritten-notes-gemini-rescue.yaml --image-case-id image-handwritten-rescue --pdf-case-id pdf-handwritten-rescue --output benchmarks/results/handwritten-notes-improve-eval-20260405-alverson-baseline.json`.
          Those fresh recipe-path reruns reproduced the current maintained
          blocker exactly: Barney stayed at `0.883604` image /
          `0.756036` pdf and corrected-scope Alverson stayed at `0.677267`
          image / `0.681952` pdf. I then screened the fresh subject-model
          candidates on the real image-entry pair only via
          `benchmarks/results/handwritten-notes-improve-eval-20260405-image-screen.json`.
          `gpt-5.4-pro` produced empty `page_html_v1` artifacts on both real
          fixtures at
          `output/runs/eval-barney-image-gpt-5-4-pro/01_ocr_ai_gpt51_v1/pages_html.jsonl`
          and
          `output/runs/eval-alverson-image-gpt-5-4-pro/01_ocr_ai_gpt51_v1/pages_html.jsonl`
          (`overall_ratio = 0.0` both), which is current
          **prompt/pipeline-wrong / integration-wrong** evidence rather than a
          usable OCR win. `gemini-3.1-pro-preview` stayed below the maintained
          rescue lane on both corrected real fixtures: Barney reached only
          `0.843434` at
          `output/runs/eval-barney-image-gemini-3-1-pro-preview/01_ocr_ai_gpt51_v1/pages_html.jsonl`,
          and corrected-scope Alverson stayed at `0.262102` at
          `output/runs/eval-alverson-image-gemini-3-1-pro-preview/01_ocr_ai_gpt51_v1/pages_html.jsonl`,
          with the same visible-source failure class (`nothing`, `Battle of
          Chickamauga`, etc.). Because no fresh model materially beat both real
          fixtures, I did not promote any candidate to a PDF-entry or full
          five-fixture rerun. Result: the `new-subject-model` retry condition
          was exercised honestly, but no current candidate justifies reopening
          Story 191.
      - id: "008"
        date: 2026-04-10
        status: failed
        approach: "Story 208 bounded GLM-OCR benchmark on the corrected Barney/Alverson pair before any handwritten reopen claim"
        score_before: 0.677267
        score_after: 0.097473
        note: >
          Story 208 benchmarked the current GLM-OCR candidate as a materially
          different OCR substrate instead of assuming handwriting wins from
          leaderboard framing. The bounded harness in
          `scripts/spikes/glm_ocr_benchmark.py` called local `ollama`
          `glm-ocr` directly on the corrected Barney and Alverson real-fixture
          pair and scored the outputs with
          `benchmarks/scorers/handwritten_notes_transcription.py`. Fresh
          story-local artifacts under
          `output/runs/story208-glm-ocr-benchmark-r1/handwritten/` recorded
          `pass_rate = 0.0`, `overall_min_ratio = 0.097473`, and
          `page_min_ratio = 0.097473` across the four entry cases
          (`barney-image = 0.283231`, `barney-pdf = 0.283231`,
          `alverson-image = 0.097473`, `alverson-pdf = 0.292624`). Manual
          artifact inspection in the same pass confirmed visible-source drift
          and hallucination rather than a narrow scoring quirk at
          `output/runs/story208-glm-ocr-benchmark-r1/handwritten/barney-image/pages_html.jsonl`
          and
          `output/runs/story208-glm-ocr-benchmark-r1/handwritten/alverson-pdf/pages_html.jsonl`.
          Result: GLM-OCR is current **new-substrate-wrong** evidence on the
          corrected handwritten blocker surface and does not justify reopening
          Story 191.
      - id: "009"
        date: 2026-05-20
        status: failed
        approach: "Screen Gemini 3.5 Flash on the corrected Barney/Alverson real image-entry pair after paid-tier availability recovered"
        score_before: 0.677267
        score_after: 0.24735
        note: >
          Initial Gemini 3.5 Flash handwritten follow-up was blocked by the same
          free-tier project quota and `503 UNAVAILABLE` high-demand responses as
          the crop validator retries. After the heartbeat retry health probe
          succeeded on the canonical doc-web key, I ran a bounded image-entry
          screen on the corrected Barney and Alverson real fixtures. The screen
          scored Barney at `0.807498`, Alverson at `0.247350`, and the
          real-fixture floor at `0.247350`, below the maintained rescue floor of
          `0.677267`. Manual inspection confirmed wrong-source Alverson text
          such as `Danville Va Nov 18th 1863` and `Battle of Chickamauga`.
          Because the candidate did not beat both corrected real fixtures, it
          was not promoted to PDF-entry or full-corpus reruns. No maintained
          recipes, prompts, scorers, or goldens were changed.
      - id: "010"
        date: 2026-05-28
        status: failed
        approach: "Screen Claude Opus 4.8 on the corrected Barney/Alverson real image-entry pair after the Anthropic release"
        score_before: 0.677267
        score_after: 0.680902
        note: >
          Claude Opus 4.8 required the Anthropic OCR client to omit
          `temperature` and send adaptive thinking/high effort for direct OCR
          module calls. The bounded image-entry screen scored Barney at
          `0.711207`, Alverson at `0.680902`, and the real-fixture floor at
          `0.680902`, with `4027` prompt tokens, `918` completion tokens, and
          estimated cost about `$0.0431` total. This barely clears the corrected
          Alverson image baseline, but it regresses Barney versus both the Story
          192 maintained image-entry evidence (`0.908567`) and the later
          improve-eval rerun (`0.883604`). Because no case approached the `0.99`
          bar and Barney materially regressed, it was not promoted to PDF-entry
          or full-corpus reruns. No maintained recipes, prompts, scorers, or
          goldens were changed.

      - id: "011"
        date: 2026-07-20
        status: failed
        approach: "Story 230 exact-weight Unlimited-OCR screen on the corrected Barney/Alverson real image pair"
        score_before: 0.677267
        score_after: 0.022946
        note: >
          Unlimited-OCR scored `0.200382` on Barney and `0.022946` on
          corrected-scope Alverson, with `0/2` passing fixtures. Manual source
          and artifact inspection classified the omissions, substitutions,
          fabricated formula, and runaway Alverson generation as candidate
          model/runtime failures; no golden or scorer changed. Story 191 stays
          blocked. Portable proof:
          `docs/evals/attempts/017-unlimited-ocr-whole-document-challenger.md`.
      - id: "012"
        date: 2026-07-21
        status: failed
        approach: "Screen Gemini 3.6 Flash and Gemini 3.5 Flash-Lite through driver.py on the corrected real image-entry pair"
        score_before: 0.677267
        score_after: 0.984052
        note: >
          The first score exposed a test-wrong issue: SequenceMatcher's default
          autojunk heuristic understated long OCR near-matches. The corrected
          scores were Gemini 3.6 Flash `0.984527` Barney / `0.984052` Alverson
          and Flash-Lite `0.974964` / `0.989935`; all remained below `0.99`,
          with manual scan review confirming genuine residual model errors.
          Story 191 remains blocked. Portable proof:
          `docs/evals/attempts/018-gemini36-flash-and-gemini35-flash-lite-bounded-challenger.md`.

    retry_when:
      - condition: new-subject-model
        note: "Retry only when a materially stronger model or OCR substrate appears that could plausibly clear Barney and the corrected Alverson front-page truth surface."

  - id: onward-table-fidelity
    name: Onward Genealogy Table Fidelity
    type: quality
    description: >
      Multi-page genealogy table OCR fidelity for Onward to the Unknown.
      Tests cell-level accuracy against hand-validated golden references
      using LCS sequence alignment to match rows.
    pipeline_stage: ocr
    runner: promptfoo
    command: "cd benchmarks && promptfoo eval -c tasks/onward-table-fidelity.yaml --no-cache -j 2"
    config: benchmarks/tasks/onward-table-fidelity.yaml
    scorer: benchmarks/scorers/html_table_diff.py
    golden: benchmarks/golden/onward/
    story_refs: ["134", "208", "230"]
    category_refs: ["spec:2"]
    compromise_refs: ["C1"]
    spec_compromise: C1
    golden_verified: true  # Hand-validated 100% correct against scans

    target:
      metric: structure_preservation
      value: 0.95

    ranking:
      quality_winner: "GPT-5.5 (score-only challenger)"
      balanced_winner: "Gemini 3.1 Pro"
      value_winner: "GPT-5.4"
      note: >
        2026-04-24 GPT-5.5 score-only challenger is the new aggregate-score
        leader at `0.9714`, narrowly above Gemini 3.1 Pro's `0.969`, but all
        three cases still have exact cell errors. Current-price cost from
        token counts is about `$1.9268` total (`$0.6423` per chapter call), so
        keep the applied recipe unchanged until failure class and value are
        reviewed. 2026-03-11 context: Gemini 3.1 Pro jumped from 0.115 to 0.969
        after the truncation fix, and GPT-5.4 remains the tracked value winner
        with known estimated cost.
      applied: >
        Recipe updated to gemini-3.1-pro-preview with maxOutputTokens=65536 (2026-03-11).
        Previous: claude-sonnet-4-6 (balanced winner from attempt 001).

    scores:
      - model: "Unlimited-OCR best-of-three exact-weight FP32 CPU screen"
        metrics:
          structure_preservation: 0.6049
          alma: 0.6797
          arthur: 0.5384
          marie_louise: 0.5966
          promptfoo_pass_rate: 0.0
          meaningful_wins: 0
          oracle_hybrid_score: 0.969
          candidate_selection_share: 0.0
        latency_ms: 168099
        cost_usd: 0.0
        cost_estimated: false
        measured: 2026-07-20
        git_sha: ac574bf
        note: >
          Story 230 measured single-page Gundam, single-page Base, and one-shot
          multi-page Base on the same source rasters, then selected the best
          transport-valid candidate arm per independently reviewed whole-case
          golden. The best candidate averaged `0.6049` versus the historical
          applied Gemini 3.1 Pro record at `0.969`, a `-0.3641` delta with
          `0/3` meaningful wins. Multi-page mode lost to the best single-page
          arm on every case and emitted seven page segments for six Arthur
          inputs. The incumbent values are historical registry evidence, not a
          fresh current-runtime rerun. Manual source/output review confirmed
          collapsed columns, merged headings, omissions, and OCR drift rather
          than a scorer/golden defect. Decision: do not adopt or add a second
          model. Portable proof:
          `docs/evals/attempts/017-unlimited-ocr-whole-document-challenger.md`.
      - model: "GPT-5.5"
        metrics:
          structure_preservation: 0.9714
          alma: 0.9333
          arthur: 0.9918
          marie_louise: 0.989
          promptfoo_pass_rate: 0.0
        latency_ms: 162210
        cost_usd: 0.6423
        cost_estimated: true
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh new-subject-model challenger rerun via
          `cd benchmarks && promptfoo eval -c tasks/onward-table-fidelity.yaml
          --no-cache --filter-providers 'openai:chat:gpt-5.5' --output
          results/gpt55-onward-table-fidelity-20260424.json -j 1`. The run
          completed all `3` cases with `0` provider errors and average
          `structure_preservation = 0.9714`, which narrowly beats the prior
          Gemini 3.1 Pro aggregate score. Promptfoo still marked `0/3` cases
          passed because the scorer requires exact cell equality; failures were
          residual row/cell errors, especially Alma child-note/descendant-count
          drift. Total tokens `192778`; estimated current GPT-5.5 API cost from
          token counts is about `$1.9268` total (`$0.6423` per chapter call).
          Portable tracked proof:
          `docs/evals/attempts/004-gpt55-onward-table-fidelity-refresh.md`.
      # --- 2026-03-11 refresh (attempt 002) ---
      - model: "Gemini 3.1 Pro"
        metrics:
          structure_preservation: 0.969
          alma: 0.923
          arthur: 0.989
          marie_louise: 0.995
        latency_ms: 207500
        cost_usd: 0.0903
        cost_estimated: true
        measured: 2026-03-11
        note: "New quality winner. maxOutputTokens=65536 required (reasoning eats budget). Slowest but cheapest of quality tier."
      - model: "Claude Opus 4.6"
        metrics:
          structure_preservation: 0.953
          alma: 0.923
          arthur: 0.964
          marie_louise: 0.973
        latency_ms: 123118
        cost_usd: 0.3175
        measured: 2026-03-11
        note: "Stable vs prior run (+0.001). 3.5x more expensive than Gemini 3.1 Pro."
      - model: "GPT-5.4"
        metrics:
          structure_preservation: 0.952
          alma: 0.907
          arthur: 0.976
          marie_louise: 0.972
        latency_ms: 102571
        cost_usd: 0.1067
        cost_estimated: true
        measured: 2026-03-11
        note: "New. Fastest model that meets 0.95 target. Value winner — Opus quality at Sonnet price."
      - model: "Claude Sonnet 4.6"
        metrics:
          structure_preservation: 0.945
          alma: 0.918
          arthur: 0.978
          marie_louise: 0.939
        latency_ms: 110051
        cost_usd: 0.1842
        measured: 2026-03-11
        note: "Stable vs prior run (-0.001). No longer balanced winner — GPT-5.4 is cheaper and higher quality."
      # --- Budget Gemini models (truncation-limited, pending re-eval) ---
      - model: "Gemini 2.5 Flash"
        metrics:
          structure_preservation: 0.886
          alma: 0.839
          arthur: 0.918
          marie_louise: 0.900
        latency_ms: 84000
        cost_usd: 0.0555
        cost_estimated: true
        measured: 2026-03-11
        note: "Best budget model. Below 0.95 target but fast and cheap. May improve with prompt tuning."
      # --- Previous scores (attempt 001, 2026-03-10) ---
      - model: "GPT-4o"
        metrics:
          structure_preservation: 0.855
        latency_ms: 688500
        cost_usd: 0.1104
        cost_estimated: true
        measured: 2026-03-10
        note: "Decent but slow (689s avg). Not competitive with Claude or GPT-5.4."
      - model: "GPT-5.1"
        metrics:
          structure_preservation: 0.847
        latency_ms: 91200
        cost_usd: 0.1155
        cost_estimated: true
        measured: 2026-03-10
        note: "Merges continuation rows. Superseded by GPT-5.4."
      - model: "Gemini 2.5 Pro"
        metrics:
          structure_preservation: 0.577
        latency_ms: 97000
        cost_usd: 0.1480
        cost_estimated: true
        measured: 2026-03-10
        note: "Merges BOY/GIRL columns despite instructions. Superseded by Gemini 3.1 Pro."

    attempts:
      - id: "001"
        date: 2026-03-10
        status: succeeded
        approach: "5-model sweep with alignment-based scorer and iterative prompt tuning"
        score_before: null
        score_after: 0.952
        note: >
          Started with broken scorer (strict positional comparison), 0.57 avg.
          Fixed scorer with LCS alignment → 0.916. Fixed prompt for section headers → 0.952.
          Key wins: (1) alignment scorer, (2) explicit section header separation, (3) Claude Opus model.
      - id: "002"
        date: 2026-03-11
        status: succeeded
        approach: "8-model refresh: GPT-5.4, GPT-5.2 Pro, Gemini 3.1 Pro, Gemini budget models (Flash, Flash-Lite) + re-run previous winners"
        score_before: 0.952
        score_after: 0.969
        note: >
          Gemini 3.1 Pro jumped from 0.115 → 0.969 after fixing maxOutputTokens (16384→65536).
          Reasoning tokens were consuming output budget, truncating tables. GPT-5.4 matched Opus
          quality at 3x lower cost. GPT-5.2 Pro is not a chat model (404). Budget Gemini models
          (Flash, Flash-Lite) scored 0.72-0.89 but truncation-limited on longer tables.
          Created enrich-eval-results.py for cost/latency/quality analysis.
          Created discover-models.ts for model discovery across providers.
      - id: "003"
        date: 2026-04-10
        status: failed
        approach: "Story 208 bounded GLM-OCR Marie-Louise seam check using the existing page-level scorer and a native-arm64 SDK control on page 080"
        score_before: null
        score_after: 0.318375
        note: >
          Story 208 did not rerun the full multi-chapter promptfoo sweep.
          Instead it used the existing `benchmarks/scorers/html_table_diff.py`
          surface on the explicit Marie-Louise table-heavy seam
          (`Image080.jpg` through `Image083.jpg`) to test whether GLM-OCR was
          even locally competitive before any adoption claim. The bounded
          direct benchmark artifacts under
          `output/runs/story208-glm-ocr-benchmark-r1/onward-marie-louise/`
          scored `glm_average_score = 0.318375`, `glm_min_score = 0.1689`,
          and `0/4` passing pages against the committed per-page goldens,
          versus incumbent scores `0.93075` average and `0.7489` minimum from
          `/Users/cam/Documents/Projects/codex-forge/output/runs/onward-full-audit-20260318-r1/02_ocr_ai_gpt51_v1/pages_html.jsonl`.
          Story 208 then proved the fuller official `glmocr` self-hosted SDK
          path in a native arm64 Python 3.14 env and rescored page 080 via
          `output/runs/story208-glm-ocr-sdk-page080-r1/summary.json`; that
          control still landed at `0.1689`, identical to the direct
          model-only page-080 result. This attempt is intentionally **not
          directly comparable** to the registry's full `structure_preservation`
          leaderboard because it is a bounded Marie-Louise seam check, but it
          is verified negative evidence that GLM-OCR is not a credible next
          Onward table candidate on the tested hard lane.
      - id: "004"
        date: 2026-04-24
        status: partial
        approach: "New-subject-model screen: add callable GPT-5.5 to the Onward table-fidelity promptfoo surface"
        score_before: 0.969
        score_after: 0.9714
        note: >
          GPT-5.5 narrowly improved the aggregate score over Gemini 3.1 Pro
          (`0.9714` vs `0.969`) and ran faster on average (`162210 ms` vs
          `207500 ms`), but the scorer still returned `0/3` exact-match passes.
          Error profile: Alma remained weakest at `0.9333` with continuation and
          child-count drift; Arthur and Marie-Louise were high-scoring but still
          had exact cell mismatches. No recipe, scorer, prompt, or golden
          changes were made; adoption remains pending value/failure review.
      - id: "005"
        date: 2026-04-24
        status: failed
        approach: "Run GPT-5.5 through the maintained Onward single-page OCR screens"
        score_before: 0.272
        score_after: 0.0
        note: >
          Added `openai:chat:gpt-5.5` to
          `benchmarks/tasks/onward-single-page-gpt54-screen.yaml` and
          `benchmarks/tasks/onward-table-fidelity-single-page-budget.yaml`,
          then ran both screens filtered to GPT-5.5. The 3-page screen wrote
          `benchmarks/results/gpt55-onward-single-page-screen-20260424.json`
          with `0/3` passes, `0` provider errors, `2786` tokens, average
          latency `3878 ms`, and estimated total cost `$0.0265`. The 15-page
          budget screen wrote
          `benchmarks/results/gpt55-onward-single-page-budget-20260424.json`
          with `0/15` passes, `0` provider errors, `14957` tokens, average
          latency `4504 ms`, and estimated total cost `$0.1633`. This is a
          prompt/scorer-contract failure on the single-page `onward_ocr.js`
          screen, not an API availability failure.
      - id: "006"
        date: 2026-04-24
        status: failed
        approach: "Re-run the suspicious three-page Onward single-page OCR screen through OpenAI Responses"
        score_before: 0.0
        score_after: 0.0
        note: >
          After adding Responses-compatible image formatting, reran
          `benchmarks/tasks/onward-single-page-gpt54-screen.yaml` filtered to
          `openai:responses:gpt-5.5` and `openai:responses:gpt-5.5-pro`.
          Both models still failed `0/3` with `Output has no tables`, so the
          single-page no-table result was not caused solely by the Chat
          Completions provider. Result file:
          `benchmarks/results/gpt55-responses-onward-single-page-screen-20260424.json`.

      - id: "007"
        date: 2026-07-20
        status: failed
        approach: "Story 230 four-arm exact-weight Unlimited-OCR whole-document challenger on all three independently reviewed Onward cases"
        score_before: 0.969
        score_after: 0.6049
        note: >
          The best Unlimited-OCR arm per case scored Alma `0.6797`, Arthur
          `0.5384`, and Marie-Louise `0.5966`, versus historical applied scores
          `0.923`, `0.989`, and `0.995`. Meaningful wins were `0/3`, the
          oracle-hybrid stayed `0.969`, and candidate selection share was `0.0`.
          Arthur multi-page also hard-failed page coverage. No maintained
          module, recipe, router, scorer, or golden changed. Portable proof:
          `docs/evals/attempts/017-unlimited-ocr-whole-document-challenger.md`.

    retry_when:
      - condition: new-subject-model
        note: "Future models may improve alma score (weakest across all models due to handwritten annotations)"
      - condition: cheaper-subject-model
        note: "Gemini Flash at 0.886 is close — prompt tuning or future Flash versions may cross 0.95"
      - condition: downsampling-sweep
        note: "Story 134 validated 2048px. Test 1500px/1024px for further cost reduction if needed."

    # --- 2026-03-12 Story 134 optimization validations ---
    downsample_validation:
      - resolution: "2048px (1582x2048 from 5100x6600)"
        model: "Gemini 3.1 Pro"
        metrics:
          structure_preservation: 0.963
          alma: 0.930
          arthur: 0.980
          marie_louise: 0.980
        measured: 2026-03-12
        note: >
          Story 134 A1 validation. Downsampled from 34MP to ~3.2MP (10.6x fewer pixels).
          Quality delta: -0.006 vs full-res baseline (0.969). Within VLM non-determinism.
          Validates max_long_side=2048 as safe default for OCR pipeline.

    single_page_budget_eval:
      - model: "GPT-5.5"
        metrics: { single_page_avg: 0.0, promptfoo_pass_rate: 0.0 }
        latency_ms: 4504
        cost_usd: 0.0109
        cost_estimated: true
        measured: 2026-04-24
        note: >
          GPT-5.5 failed the maintained 15-page single-page budget screen
          (`0/15` promptfoo passes, `0` provider errors) via
          `benchmarks/results/gpt55-onward-single-page-budget-20260424.json`.
          The narrower 3-page GPT-5.4 Mini/Nano screen also failed `0/3` via
          `benchmarks/results/gpt55-onward-single-page-screen-20260424.json`.
          Current-price estimated cost was `$0.0109` per page on the 15-page
          screen (`$0.1633` total).
      - model: "Gemini 2.5 Flash"
        metrics: { single_page_avg: 0.257 }
        measured: 2026-03-12
        note: "Story 134 A7. Per-page table quality vs Gemini 3.1 Pro golden. Not viable for table content."
      - model: "Gemini 3 Flash"
        metrics: { single_page_avg: 0.103 }
        measured: 2026-03-12
        note: "Worst performer. 2 pages produced no tables at all."
      - model: "Gemini 3.1 Flash-Lite"
        metrics: { single_page_avg: 0.271 }
        measured: 2026-03-12
        note: "Similar to 2.5 Flash. Budget models genuinely worse at table OCR, not just truncation-limited."
      - model: "GPT-5 Mini"
        metrics: { single_page_avg: 0.272 }
        measured: 2026-03-12
        note: "OpenAI budget model. Same quality tier as Gemini Flash-Lite."

  - id: onward-document-consistency-planning
    name: Onward Document Consistency Planning
    type: quality
    description: >
      Manual eval for the document-level planning artifacts emitted after page-scope
      extraction on Onward genealogy chapters. Measures whether the planner surfaces
      manually observed format-drift classes better than the chapter-first detector
      and keeps row-semantic issues distinct from pure format drift.
    pipeline_stage: validate
    runner: manual
    story_refs: ["144", "146", "219"]
    category_refs: ["spec:1", "spec:2", "spec:5"]
    compromise_refs: ["C7"]
    spec_compromise: C7

    target:
      metric: missed_manual_format_coverage
      value: 1.0

    scores:
      - model: "Story 144 planner (gpt-5 primary, gpt-4.1 fallback)"
        metrics:
          missed_manual_format_coverage: 1.0
          chapter_009_not_pure_format: 1.0
        measured: 2026-03-15
        note: >
          Story 144 revalidated on `story144-onward-document-consistency-plan-r7`
          after hardening mixed-summary bucketing and dossier-supported issue-type
          normalization.
          The `conformance_report` surfaced the previously missed manual format-failure
          chapters `011/012/013/014/018/019/020`, kept `chapter-010.html` flagged,
          routed `chapter-009.html` into a mixed row-semantic-containing bucket
          instead of pure format drift, and restored `chapter-023.html` to
          conformant baseline status.
      - model: "Story 146 planner-guided rerun loop (gpt-5 rerun stage)"
        metrics:
          missed_manual_format_coverage: 1.0
          chapter_009_not_pure_format: 1.0
          remaining_format_issue_count: 19
          remaining_format_issue_delta: -2
        measured: 2026-03-15
        note: >
          Story 146 validation run `story146-onward-plan-aware-genealogy-reruns-r1`
          consumed `pattern_inventory`, `consistency_plan`, and `conformance_report`
          inside the rerun loop, accepted `11/18` targeted pages, preserved
          `chapter-009.html` as mixed and `chapter-023.html` as conformant, and
          lowered remaining pure-format issue-type count from `21` to `19` by
          clearing `fragmented_multi_table_chapter` from `chapter-018.html` and
          `chapter-019.html` plus `missing_subgroup_rows` from `chapter-017.html`,
          although the pure-format chapter set itself did not shrink.
      - model: "Story 219 maintained-lane remeasure + structured repair target (gpt-5 planner/rerun/structured)"
        metrics:
          missed_manual_format_coverage: 1.0
          chapter_009_not_pure_format: 1.0
          maintained_format_drift_chapter_count: 0
          maintained_row_semantic_issue_chapter_count: 2
        measured: 2026-04-12
        note: >
          Story 219 validation run `story219-onward-structured-repair-r1`
          reused the maintained `onward-book-r1` artifacts, rebuilt chapters,
          and re-ran the planner before the direct-HTML rerun, after the
          direct-HTML rerun, and after the new structured repair stage.
          The current maintained seam already has no `format_drift` chapters
          before the direct-HTML rerun; only `chapter-009.html` and
          `chapter-022.html` remain as `row_semantic_issue`, while
          `chapter-023.html` stays conformant. The structured repair stage
          landed but received `0` planner-selected pages, so the old Story 146
          pure-format plateau is no longer the active maintained-lane truth.

    attempts:
      - id: "001"
        date: 2026-03-15
        status: succeeded
        approach: "Story 144 document-level dossier + AI planning pass + normalized conformance report"
        note: >
          Driver run `story144-onward-document-consistency-plan-r5` emitted
          `pattern_inventory`, `consistency_plan`, and `conformance_report`
          sidecars and outperformed the existing chapter-first detector on the
          reviewed missed-format set.
      - id: "002"
        date: 2026-03-15
        status: succeeded
        approach: "Story 144 post-validate hardening for mixed-summary bucketing and dossier-supported issue typing"
        note: >
          Driver run `story144-onward-document-consistency-plan-r7` preserved the
          missed-format coverage while removing `chapter-009.html` from pure-format
          summary buckets and preventing unsupported AI issue-type hallucinations
          from flagging clean `chapter-023.html`.
      - id: "003"
        date: 2026-03-15
        status: partial
        approach: "Story 146 plan-aware reruns consume planning sidecars and re-plan after bounded source-aware rereads"
        note: >
          Driver run `story146-onward-plan-aware-genealogy-reruns-r1` proved the
          emitted planning artifacts are actionable input to the rerun stage and
          preserved the mixed/conformant guardrails, but direct-HTML reruns only
          reduced remaining pure-format issue-type count (`21 -> 19`) instead of
          shrinking the pure-format chapter set.
      - id: "004"
        date: 2026-04-12
        status: partial
        approach: "Story 219 maintained-lane remeasure plus bounded structured/row-oriented repair target"
        note: >
          Driver run `story219-onward-structured-repair-r1` proved the new
          structured repair lane can run end-to-end on the maintained seam, but
          the fresh maintained baseline no longer exposes a pure-format chapter
          set at all: `format_drift_chapters` stayed empty before and after the
          direct-HTML rerun, and the structured lane received `0` planner-selected
          pages. The remaining maintained issues are row-semantic note-placement
          defects in `chapter-009.html` and `chapter-022.html`, so this eval no
          longer points to an active C7 format-drift gap on maintained Onward.

    retry_when:
      - condition: new-approach
        note: "Only retry this C7 lane if a maintained repeated-structure target exposes `format_drift` again or a second repeated-structure document is packaged with the same planner/conformance surface."
      - condition: architecture-change
        note: "If we decide to attack row-semantic note placement as the active maintained gap, track it in a separate story/eval instead of reusing this pure-format planning eval."

  - id: onward-genealogy-row-semantic-note-placement
    name: Onward Genealogy Row-Semantic Note Placement
    type: quality
    description: >
      Manual eval for the maintained Onward child-note placement seam.
      Reuses the maintained `onward-book-r1` artifacts, runs the bounded
      Story 220 structured-repair recipe, and checks that the maintained
      `row_semantic_issue` chapters leave that bucket without regressing the
      clean guardrail chapter.
    pipeline_stage: validate
    runner: manual
    command: "find modules tests -name \"*.pyc\" -delete && python driver.py --recipe configs/recipes/story-220-onward-genealogy-row-semantic-note-placement-validate.yaml --run-id story220-onward-row-semantic-r7 --allow-run-id-reuse --force"
    story_refs: ["220"]
    category_refs: ["spec:2", "spec:3", "spec:5", "spec:6", "spec:7"]
    spec_refs: ["spec:2.1", "spec:3.1", "spec:5.1"]
    compromise_refs: ["C7"]
    spec_compromise: C7

    target:
      metric: pass_rate
      value: 1.0
      note: >
        The maintained row-semantic seam passes only when the final
        `conformance_report` has empty `row_semantic_issue_chapters`,
        repaired chapter HTML shows the moved note rows outside `DIED`,
        and `chapter-023.html` remains conformant.

    scores:
      - model: "Story 220 maintained structured row-semantic repair proof (gpt-4.1 planner/repair)"
        metrics:
          pass_rate: 1.0
          row_semantic_issue_chapter_count: 0
          targeted_page_count: 5
          accepted_page_count: 5
        measured: 2026-04-13
        note: >
          Fresh cache-purged driver run `story220-onward-row-semantic-r7`
          reused the
          maintained `onward-book-r1` artifacts, targeted pages
          `23,24,25,26,111`, and accepted `5/5` structured repairs. The final
          conformance report
          `output/runs/story220-onward-row-semantic-r7/09_plan_onward_document_consistency_v1/conformance_report_after_structured_repair.json`
          left `format_drift_chapters`, `mixed_issue_chapters`, and
          `row_semantic_issue_chapters` empty. Manual HTML inspection confirmed
          `chapter-009.html` moved `1-Kassandra`, `born Dec 30/1999`,
          `Joseph born Feb 4/`, `Tessa born June 2/91 May 15 Chelsea wa`, and
          `born to Michelle &` into subordinate note rows outside `DIED`;
          `chapter-022.html` moved `6 months old` out of `DIED`; and
          `chapter-023.html` preserved `TOTAL DESCENDANTS | 38`.

    attempts:
      - id: "001"
        date: 2026-04-13
        status: succeeded
        approach: "Story 220 gpt-4.1 planner/structured repair with widened page targeting and note-row normalization"
        note: >
          Early proof runs exposed three blockers in sequence: OpenAI Responses
          scopes on `gpt-5`, target truncation after widening the relevant page
          cluster, and note-placement regressions from structured `DIED` cells
          plus shared genealogy HTML normalization. Story 220 switched the proof
          recipe to `gpt-4.1`, widened the target set to pages
          `23,24,25,26,111`, augmented planner clusters for
          `child_note_in_wrong_column`, and normalized non-death note text into
          subordinate rows. The closing cache-purged run
          `story220-onward-row-semantic-r7`
          cleared the maintained seam.

    retry_when:
      - condition: new-approach
        note: >
          Retry when a broader maintained row-semantic note class appears that
          the current subordinate-note policy does not cover.
      - condition: architecture-change
        note: >
          Re-measure if planner issue typing, structured-repair rendering, or
          the late genealogy HTML normalization path changes materially.

  # ── Compromise Detection Evals ───────────────────────────────────────────

  - id: single-model-crop-detection
    name: "C4 Detection: Single-Model Crop Quality"
    type: compromise
    description: >
      Can a single model replace the two-stage detector+validator?
      Tests whether one call achieves ≥0.95 with zero text contamination.
    story_refs: ["207"]
    category_refs: ["spec:4"]
    compromise_refs: ["C4"]
    spec_compromise: C4
    runner: promptfoo

    target:
      metric: overall
      value: 0.95
      pass_rate: 0.95

    scores:
      - model: "Gemini 3 Flash (conservative-count prompt, maintained rerun)"
        metrics:
          overall: 0.9703
          pass_rate: 1.0
        measured: 2026-04-10
        git_sha: d074136
        note: >
          Story 207 reran the maintained single-stage candidate through the
          same filtered `image-crop-extraction` surface and wrote
          `benchmarks/results/story207-image-crop-baseline.json`. The rerun
          again cleared `13/13` scorer passes, and recomputing the mean from
          the per-case scores yielded `0.9703 / 1.0`
          (overall / pass_rate). The portable tracked proof anchor is
          `docs/evals/attempts/001-image-crop-extraction-story207-proof-refresh.md`;
          this is now the current canonical C4 deletion-gate proof.
      - model: "GPT-5.5 (conservative-count prompt)"
        metrics:
          overall: 0.8111
          pass_rate: 0.7692
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Fresh new-subject-model challenger rerun through the same maintained
          filtered detector surface wrote
          `benchmarks/results/gpt55-image-crop-extraction-20260424.json` and
          reached `10/13` scorer passes with `overall = 0.8111` and
          `pass_rate = 0.7692`. The failures were bbox precision misses on
          `Image001`, `Image013`, and `Image020`, so this does not replace the
          maintained Gemini 3 Flash detector. Portable tracked proof:
          `docs/evals/attempts/003-gpt55-crop-eval-refresh.md`.
      - model: "GPT-5.5 Responses (conservative-count prompt)"
        metrics:
          overall: 0.791
          pass_rate: 0.7692
        measured: 2026-04-24
        git_sha: aa5b577
        note: >
          Direct Responses API rerun through the same maintained filtered
          detector surface wrote
          `benchmarks/results/gpt55-responses-image-crop-extraction-gpt55-20260424.json`
          and reached `10/13` scorer passes with `overall = 0.791` and
          `pass_rate = 0.7692`. The failure set shifted to `Image001`,
          `Image021`, and `Image059`, but the gate still fails. Portable
          tracked proof: `docs/evals/attempts/006-gpt55-responses-eval-refresh.md`.
      - model: "Gemini 3 Flash (conservative-count prompt, heading-safe revision)"
        metrics:
          overall: 0.9678
          pass_rate: 1.0
        measured: 2026-04-03
        git_sha: b1a9964
        note: >
          Fresh verified rerun after tightening the maintained
          `conservative-count` prompt to exclude heading-style display text
          that belongs to the reading flow. Result file:
          `benchmarks/results/single-model-crop-detection-g3flash-20260403-promptfix.json`.
          This clears the C4 deletion gate on the maintained 13-page corpus.
      - model: "Gemini 3 Flash (conservative-count prompt, single-stage)"
        metrics:
          overall: 0.9321
          pass_rate: 1.0
        measured: 2026-04-03
        git_sha: b1a9964
        note: >
          Fresh comparable rerun on the maintained single-stage candidate via
          `benchmarks/results/single-model-crop-detection-g3flash-20260403.json`.
          The run cleared `13/13` scorer passes and improved the stale C4 gate
          materially, but it still missed the `0.95` overall threshold because
          `Image000` undercovers the full cover and five mixed photo/title
          pages remain ambiguous under the current prompt/golden contract.
      - model: "Gemini 3 Pro (single-stage, no validator)"
        metrics:
          overall: 0.856
          pass_rate: 0.77
        measured: 2026-01-25
        note: "Not yet passing — validator still needed"

    attempts:
      - id: "001"
        date: 2026-04-03
        status: partial
        approach: "Refresh the stale C4 gate on the maintained single-stage Gemini 3 Flash candidate"
        score_before: 0.856
        score_after: 0.9321
        note: >
          Comparable rerun on `b1a9964` used the maintained
          `image-crop-extraction` task filtered to Gemini 3 Flash +
          `conservative-count` and wrote
          `benchmarks/results/single-model-crop-detection-g3flash-20260403.json`.
          Result: `13/13` scorer passes, `overall = 0.9321`,
          `pass_rate = 1.0`, average latency about `10845 ms` per page, and
          total eval cost about `$0.084`. Classification: one clear
          model-wrong full-cover undercrop (`Image000`) plus five ambiguous
          extra title-text crops (`Image011`, `Image013`, `Image021`,
          `Image037`, `Image059`). No golden changes were made in this pass.
      - id: "002"
        date: 2026-04-03
        status: succeeded
        approach: "Refine the maintained single-stage prompt to exclude heading-style display text, then rerun the C4 gate"
        score_before: 0.9321
        score_after: 0.9678
        note: >
          Source-page review showed the five ambiguous overcounts were
          prompt-contract drift rather than golden defects: they were ordinary
          headings and certificate title lines, not standalone visual text
          elements. Tightening `benchmarks/prompts/crop-conservative-count.js`
          yielded `13/13` scorer passes with `overall = 0.9678`,
          `pass_rate = 1.0`, average latency about `6441 ms` per page, and
          total eval cost about `$0.050`. The remaining weak case is
          `Image000`, but the deletion gate now passes on the maintained corpus.

      - id: "003"
        date: 2026-04-10
        status: succeeded
        approach: "Story 207: rerun the maintained single-stage candidate so the canonical C4 proof no longer depends on missing older result files"
        score_before: 0.9678
        score_after: 0.9703
        note: >
          Fresh current-pass rerun reused the maintained filtered task and wrote
          `benchmarks/results/story207-image-crop-baseline.json`. The run
          completed with `13/13` scorer passes, `0` failures, `0` errors,
          average latency about `7878 ms` per page, and total eval cost about
          `$0.059`; recomputing the mean from the per-case scores yielded
          `overall = 0.9703` and `pass_rate = 1.0`. `Image000` remains the
          lone materially weak case, but the maintained aggregate improves
          slightly while keeping the same honest prompt/golden contract. Portable
          tracked proof:
          `docs/evals/attempts/001-image-crop-extraction-story207-proof-refresh.md`.
      - id: "004"
        date: 2026-04-24
        status: failed
        approach: "New-subject-model screen: add callable GPT-5.5 to the maintained single-model crop detection surface"
        score_before: 0.9703
        score_after: 0.8111
        note: >
          GPT-5.5 is callable but underperformed the current Gemini 3 Flash
          single-stage detector on the same maintained `conservative-count`
          slice. It failed `Image001`, `Image013`, and `Image020` on bbox
          precision, with no scorer or golden changes. Portable tracked proof:
          `docs/evals/attempts/003-gpt55-crop-eval-refresh.md`.
      - id: "005"
        date: 2026-04-24
        status: failed
        approach: "Re-run the maintained single-model crop detector through OpenAI Responses"
        score_before: 0.8111
        score_after: 0.791
        note: >
          The Responses path changed GPT-5.5 behavior but did not improve the
          gate. It still passed only `10/13`, now failing `Image001`,
          `Image021`, and `Image059`. GPT-5.5 Pro Responses passed only a
          one-case detector smoke before full detector attempts proved too slow
          for the interactive promptfoo loop.

    retry_when:
      - condition: new-subject-model
        note: "Next-gen Gemini or GPT may achieve single-stage quality"

  - id: auto-book-type-detection
    name: "C2 Detection: Auto Book Type Detection"
    type: compromise
    description: >
      Can the maintained recommendation-only intake lane auto-detect book type
      and recommend the right maintained explicit recipe with no metadata hints?
      Tests 10 diverse PDF documents via the final `intake_plan_v1` artifact.
      Maintained direct-entry `docx` / `xlsx` / `pptx` / `web-page` / `epub`
      / `email-eml` / `email-mbox` / `mixed-archive` / `mixed-folder` lanes
      sit outside this recommendation-only automation surface. The office-family
      direct-entry boundary is measured with
      `benchmarks/input/office-intake-boundary-corpus.json`; newer non-office
      direct-entry lanes are verified through explicit scope-blocked
      recommendation-only probe rows.
    story_refs: ["169", "170", "171", "194", "197", "200", "201", "202", "203"]
    category_refs: ["spec:1", "spec:3", "spec:6", "spec:7", "spec:8", "spec:9"]
    compromise_refs: ["C2"]
    spec_compromise: C2
    runner: manual

    target:
      metric: accuracy
      value: 1.0
      note: "Must correctly classify all 10 benchmark documents"

    scores:
      - model: "Current-head recommendation-only intake rerun after direct-entry surface refresh"
        metrics:
          accuracy: 1.0
          overall: 1.0
          pass_rate: 1.0
        measured: 2026-04-10
        git_sha: 601353b
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --output benchmarks/results/auto-book-type-detection-improve-eval-20260410.json --run-root output/runs/auto-book-type-detection-improve-eval-20260410`
          kept the locked 10-case recommendation-only PDF corpus green on
          current `HEAD` (`601353b`): `accuracy = 1.0`, `overall = 1.0`, and
          `pass_rate = 1.0`. Manual inspection in the same pass reconfirmed a
          representative launchable `cyoa` row at
          `output/runs/auto-book-type-detection-improve-eval-20260410/tbotb-mini/overview_plan_final.jsonl`,
          a representative explicit `no-recipe-needed` novel row at
          `output/runs/auto-book-type-detection-improve-eval-20260410/scanned-prose-mini/overview_plan_final.jsonl`,
          and the maintained genealogy route at
          `output/runs/auto-book-type-detection-improve-eval-20260410/onward-unknown/overview_plan_final.jsonl`.
          Additional temporary boundary probing wrote
          `benchmarks/results/auto-book-type-detection-direct-entry-boundary-20260410.json`,
          confirming that `web-page`, `epub`, `email-eml`, and `email-mbox`
          still block with `scope_policy = direct_explicit_recipe_only`
          instead of silently widening the recommendation-only automation
          surface.
      - model: "Story 171 maintained intake harness (gpt-4.1-mini overview/zoom)"
        metrics:
          accuracy: 1.0
          overall: 1.0
          pass_rate: 1.0
        measured: 2026-03-29
        note: >
          Story 171 reran the repo-owned
          `benchmarks/scripts/run_auto_book_type_detection_eval.py` harness
          after adding the maintained non-TOC born-digital PDF recipe and
          updating the locked 10-document corpus expectations for `rfp` and
          `release-forms`. Detection quality still clears the benchmark after
          widening the maintained routing surface. Result file:
          `benchmarks/results/auto-book-type-detection-story171.json`.
      - model: "Story 170 maintained intake harness (gpt-4.1-mini overview/zoom)"
        metrics:
          accuracy: 1.0
          overall: 1.0
          pass_rate: 1.0
        measured: 2026-03-29
        note: >
          Story 170 reran the repo-owned
          `benchmarks/scripts/run_auto_book_type_detection_eval.py` harness
          after narrowing the maintained PDF recommendation surface so short
          flat PDFs now return `no-recipe-needed` instead of silently pointing
          at full HTML recipes that fail later at `portionize_toc`. Result
          file: `benchmarks/results/auto-book-type-detection-story170.json`.
      - model: "Story 169 maintained intake harness (gpt-4.1-mini overview/zoom)"
        metrics:
          accuracy: 1.0
          overall: 1.0
          pass_rate: 1.0
        measured: 2026-03-28
        note: >
          Story 169 added the repo-owned harness
          `benchmarks/scripts/run_auto_book_type_detection_eval.py` plus a
          10-document corpus and scored the final `intake_plan_v1` artifacts.
          The maintained recommendation-only chain classified all 10 documents
          correctly and recommended the expected maintained explicit recipe in
          every case. Result file:
          `benchmarks/results/auto-book-type-detection-story169.json`.

    attempts:
      - id: "001"
        date: 2026-03-28
        status: failed
        approach: "Story 169 pre-fix baseline on the legacy-drifting intake chain"
        note: >
          Baseline harness run on 10 documents completed end-to-end but failed
          the actual C2 job: `10/10` rows collapsed to `book_type: mixed`,
          `10/10` recommended `configs/recipes/legacy/recipe-ocr.yaml`, and
          `4/10` emitted no `signal_evidence`. Evidence:
          `output/runs/story169-baseline-current-chain/baseline_summary.json`.
      - id: "002"
        date: 2026-03-28
        status: succeeded
        approach: "Story 169 maintained recommendation-only intake harness on the locked 10-document corpus"
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --output benchmarks/results/auto-book-type-detection-story169.json`
          finished `10/10` docs with `accuracy = 1.0`, `overall = 1.0`, and
          `pass_rate = 1.0`. Sample passing artifacts include
          `output/runs/auto-book-type-detection/tbotb-mini/overview_plan_final.jsonl`,
          `output/runs/auto-book-type-detection/onward-unknown/overview_plan_final.jsonl`,
          and `output/runs/auto-book-type-detection/rfp/overview_plan_final.jsonl`.
      - id: "003"
        date: 2026-03-29
        status: succeeded
        approach: "Story 170 rerun after narrowing flat/non-book PDF recommendations to no-recipe-needed"
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --output benchmarks/results/auto-book-type-detection-story170.json`
          again finished `10/10` docs with `accuracy = 1.0`, `overall = 1.0`,
          and `pass_rate = 1.0`. Sample passing artifacts include
          `output/runs/auto-book-type-detection/scanned-prose-mini/overview_plan_final.jsonl`
          (`novel` -> `no-recipe-needed`),
          `output/runs/auto-book-type-detection/rfp/overview_plan_final.jsonl`
          (`other` -> `no-recipe-needed`), and
          `output/runs/auto-book-type-detection/astrosmash-manual/overview_plan_final.jsonl`
          (`other` + `tables/images` -> `configs/recipes/recipe-pdf-ocr-html-mvp.yaml`).
      - id: "004"
        date: 2026-03-29
        status: failed
        approach: "Story 171 pre-golden rerun after adding the maintained non-TOC born-digital recipe"
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --output benchmarks/results/auto-book-type-detection-story171-preupdate.json`
          finished `10/10` docs with no runtime failures but scored only
          `accuracy = 0.8`, `overall = 0.9167`, and `pass_rate = 0.8`
          because the locked corpus still expected `rfp` and `release-forms`
          to recommend `no-recipe-needed`. Mismatch classification:
          **golden-wrong**. The maintained intake chain now correctly
          recommends `configs/recipes/recipe-born-digital-pdf-non-toc-html-mvp.yaml`
          for those two flat born-digital PDFs after Story 171's new lane.
      - id: "005"
        date: 2026-03-29
        status: succeeded
        approach: "Story 171 rerun after widening the locked corpus to the maintained non-TOC born-digital lane"
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --output benchmarks/results/auto-book-type-detection-story171.json`
          again finished `10/10` docs with `accuracy = 1.0`, `overall = 1.0`,
          and `pass_rate = 1.0`. Sample passing artifacts include
          `output/runs/auto-book-type-detection/tbotb-mini/overview_plan_final.jsonl`
          (`cyoa` -> `configs/recipes/recipe-born-digital-pdf-marker-lite-html-mvp.yaml`),
          `output/runs/auto-book-type-detection/rfp/overview_plan_final.jsonl`
          (`other` -> `configs/recipes/recipe-born-digital-pdf-non-toc-html-mvp.yaml`), and
          `output/runs/auto-book-type-detection/release-forms/overview_plan_final.jsonl`
          (`other` + `forms` -> `configs/recipes/recipe-born-digital-pdf-non-toc-html-mvp.yaml`).
      - id: "006"
        date: 2026-04-07
        status: succeeded
        approach: "Story 194 office-family boundary probe on repo-owned DOCX/XLSX/PPTX fixtures"
        note: >
          Story 194 first reran a temporary 3-case office corpus through
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py`
          and confirmed the old failure mode: all three rows (`docx-mini`,
          `xlsx-mini`, `pptx-mini`) died at `failure_step = builder` with
          `NotADirectoryError` because the recommendation-only harness still
          treated non-PDF inputs as image directories. The story then added an
          explicit scope boundary plus the rerunnable probe corpus
          `benchmarks/input/office-intake-boundary-corpus.json` and reran
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --corpus benchmarks/input/office-intake-boundary-corpus.json --output benchmarks/results/auto-book-type-detection-story194-office-boundary.json --run-root output/runs/story194-auto-book-type-detection-office-boundary`.
          The fresh result file
          `benchmarks/results/auto-book-type-detection-story194-office-boundary.json`
          now records three inspectable blocked rows with `failure_step =
          scope`: `docx` and `xlsx` report
          `scope_policy = direct_explicit_recipe_only`, while `pptx` reports
          `scope_policy = runtime_blocked`. Sample evidence inspected in the
          same pass includes the `docx-mini` row with
          `boundary_reason = outside_recommendation_only_intake:docx:direct_explicit_recipe_only`
          and the `pptx-mini` row with
          `boundary_reason = outside_recommendation_only_intake:pptx:runtime_blocked`.
      - id: "007"
        date: 2026-04-08
        status: succeeded
        approach: "Story 197 office-family boundary probe rerun after the maintained PPTX direct-entry lane landed"
        note: >
          Story 197 added the maintained PPTX extra, runtime wiring, and
          bounded `recipe-pptx-html-mvp.yaml` lane, then reran
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --corpus benchmarks/input/office-intake-boundary-corpus.json --output benchmarks/results/auto-book-type-detection-story197-office-boundary.json --run-root output/runs/story197-auto-book-type-detection-office-boundary`.
          The fresh result file
          `benchmarks/results/auto-book-type-detection-story197-office-boundary.json`
          now records three inspectable blocked rows with `failure_step =
          scope`, and all three office kinds (`docx`, `xlsx`, `pptx`) now
          report `scope_policy = direct_explicit_recipe_only`. Sample evidence
          inspected in the same pass includes the `pptx-mini` row with
          `boundary_reason = outside_recommendation_only_intake:pptx:direct_explicit_recipe_only`.
      - id: "008"
        date: 2026-04-10
        status: succeeded
        approach: "Current-head recommendation-only intake refresh after Stories 200-203 widened direct-entry families"
        score_before: 1.0
        score_after: 1.0
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_auto_book_type_detection_eval.py --output benchmarks/results/auto-book-type-detection-improve-eval-20260410.json --run-root output/runs/auto-book-type-detection-improve-eval-20260410`
          completed the locked 10-case PDF corpus with `accuracy = 1.0`,
          `overall = 1.0`, `pass_rate = 1.0`, and `failed_runs = 0` on
          `601353b`. The newer direct-entry families remained outside the
          automation surface by design: temporary boundary probing in
          `benchmarks/results/auto-book-type-detection-direct-entry-boundary-20260410.json`
          confirmed `web-page`, `epub`, `email-eml`, and `email-mbox` all
          return `failure_step = scope` with
          `scope_policy = direct_explicit_recipe_only`, so no golden or scorer
          change was needed.

    retry_when:
      - condition: new-input-family
        note: >
          Expand or rerun when a new maintained intake family lands, or when
          routing changes could regress the final `intake_plan_v1` surface.

  - id: approved-intake-handoff
    name: "C2 Proof: Approved Intake Handoff"
    type: compromise
    description: >
      After `confirm_plan_v1` approves an `intake_plan_v1`, can the maintained
      confirmed-handoff path launch the exact approved explicit recipe without
      manually retyping the recipe path or source input? Driver-backed proof on
      a dedicated 11-case approved-handoff corpus now inspects the stamped
      `intake_handoff_v1` artifact for all 11 cases and the first downstream
      stamped artifact for the 7 launchable maintained-recipe cases. The
      recommendation-only `auto-book-type-detection` corpus stays locked at 10
      PDF cases; this surface adds one repo-owned image-directory launch case
      (`testdata/handwritten-notes-mini-images`) without widening that separate
      benchmark. Maintained direct-entry `docx` / `xlsx` / `pptx` / `web-page`
      / `epub` / `email-eml` / `email-mbox` / `mixed-archive` / `mixed-folder`
      lanes remain outside this automation surface. The office-family
      direct-entry boundary is measured with
      `benchmarks/input/office-intake-boundary-corpus.json`; newer non-office
      direct-entry lanes are verified through explicit scope-blocked
      approved-handoff probe rows.
    story_refs: ["176", "178", "180", "194", "197", "200", "201", "202", "203", "205", "218"]
    category_refs: ["spec:1", "spec:3", "spec:6", "spec:7", "spec:8", "spec:9"]
    compromise_refs: ["C2"]
    spec_compromise: C2
    runner: manual
    command: "python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-improve-eval-20260413.json --run-root output/runs/approved-intake-handoff-improve-eval-20260413"
    golden: benchmarks/golden/approved-intake-handoff/corpus.json

    target:
      metric: pass_rate
      value: 1.0
      note: "All 11 approved-handoff corpus cases must stamp the expected handoff outcome; the 7 launch cases must also stamp the expected first downstream artifact."

    scores:
      - model: "Current-head approved-handoff corpus rerun after Story 218 mixed-folder workflow change"
        metrics:
          pass_rate: 1.0
          launched: 7
          skipped: 4
          failed_runs: 0
        measured: 2026-04-13
        git_sha: 38c78a8
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-improve-eval-20260413.json --run-root output/runs/approved-intake-handoff-improve-eval-20260413`
          kept the maintained 11-case surface green on current `HEAD`
          (`38c78a8`): `pass_rate = 1.0`, `overall = 1.0`, `launched = 7`,
          `skipped = 4`, and `failed_runs = 0`. Manual inspection in the same
          pass reconfirmed a representative launched PDF row at
          `output/runs/approved-intake-handoff-improve-eval-20260413/tbotb-mini/intake_handoff.jsonl`
          plus
          `output/runs/approved-intake-handoff-improve-eval-20260413-tbotb-mini-recipe-born-digital-pdf-marker-lite-html-mvp/01_extract_pdf_marker_lite_html_v1/pages_html.jsonl`
          (first row HTML starts with `<h1 ...>The Brass Key`), a
          representative explicit skip at
          `output/runs/approved-intake-handoff-improve-eval-20260413/scanned-prose-mini/intake_handoff.jsonl`,
          and the repo-owned image-directory launch at
          `output/runs/approved-intake-handoff-improve-eval-20260413/handwritten-notes-mini-images/intake_handoff.jsonl`
          plus
          `output/runs/approved-intake-handoff-improve-eval-20260413-handwritten-notes-mini-images-recipe-images-ocr-html-mvp/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`
          (first row points at `testdata/handwritten-notes-mini-images/page-001.png`).
          Additional same-pass boundary probing now uses the committed corpus
          `benchmarks/input/non-office-direct-entry-boundary-corpus.json` and
          wrote
          `benchmarks/results/approved-intake-handoff-direct-entry-boundary-20260413.json`
          plus
          `benchmarks/results/auto-book-type-detection-direct-entry-boundary-20260413.json`,
          confirming that `web-page`, `epub`, `email-eml`, `email-mbox`,
          `mixed-archive`, and `mixed-folder` all still block with
          `scope_policy = direct_explicit_recipe_only` instead of silently
          widening either automation surface.
      - model: "Current-head approved-handoff corpus rerun after direct-entry surface refresh"
        metrics:
          pass_rate: 1.0
          launched: 7
          skipped: 4
          failed_runs: 0
        measured: 2026-04-10
        git_sha: 601353b
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-improve-eval-20260410.json --run-root output/runs/approved-intake-handoff-improve-eval-20260410`
          kept the maintained 11-case surface green on current `HEAD`
          (`601353b`): `pass_rate = 1.0`, `overall = 1.0`, `launched = 7`,
          `skipped = 4`, and `failed_runs = 0`. Manual inspection in the same
          pass reconfirmed a representative launched PDF row at
          `output/runs/approved-intake-handoff-improve-eval-20260410/tbotb-mini/intake_handoff.jsonl`
          plus
          `output/runs/approved-intake-handoff-improve-eval-20260410-tbotb-mini-recipe-born-digital-pdf-marker-lite-html-mvp/01_extract_pdf_marker_lite_html_v1/pages_html.jsonl`,
          a representative explicit skip at
          `output/runs/approved-intake-handoff-improve-eval-20260410/scanned-prose-mini/intake_handoff.jsonl`,
          and the repo-owned image-directory launch at
          `output/runs/approved-intake-handoff-improve-eval-20260410/handwritten-notes-mini-images/intake_handoff.jsonl`
          plus
          `output/runs/approved-intake-handoff-improve-eval-20260410-handwritten-notes-mini-images-recipe-images-ocr-html-mvp/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`.
          Additional temporary boundary probing wrote
          `benchmarks/results/approved-intake-handoff-direct-entry-boundary-20260410.json`,
          confirming that `web-page`, `epub`, `email-eml`, and `email-mbox`
          still block with `scope_policy = direct_explicit_recipe_only`
          instead of silently widening the automation surface.
      - model: "Story 180 dedicated approved-handoff corpus"
        metrics:
          pass_rate: 1.0
          launched: 7
          skipped: 4
          failed_runs: 0
        measured: 2026-04-02
        note: >
          Story 180 split approved handoff onto its own corpus file
          (`benchmarks/golden/approved-intake-handoff/corpus.json`) and reran
          the widened 11-case surface via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-story180.json --run-root output/runs/story180-approved-intake-handoff`.
          All 11 cases wrote stamped `intake_handoff_v1` rows; the 7 maintained
          launch cases also stamped the expected first downstream artifact under
          bounded `--end-at` execution. Manual inspection in the same pass
          confirmed the new repo-owned image-directory row at
          `output/runs/story180-approved-intake-handoff/handwritten-notes-mini-images/intake_handoff.jsonl`
          plus
          `output/runs/story180-approved-intake-handoff-handwritten-notes-mini-images-recipe-images-ocr-html-mvp/01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`,
          a representative launched PDF row at
          `output/runs/story180-approved-intake-handoff/tbotb-mini/intake_handoff.jsonl`
          plus
          `output/runs/story180-approved-intake-handoff-tbotb-mini-recipe-born-digital-pdf-marker-lite-html-mvp/01_extract_pdf_marker_lite_html_v1/pages_html.jsonl`,
          and a representative explicit skip at
          `output/runs/story180-approved-intake-handoff/scanned-prose-mini/intake_handoff.jsonl`.
      - model: "Story 178 corpus-wide confirmed-handoff harness"
        metrics:
          pass_rate: 1.0
          launched: 6
          skipped: 4
          failed_runs: 0
        measured: 2026-04-02
        note: >
          Story 178 added a dedicated corpus-wide approved-handoff harness and
          reran the locked 10-document maintained intake corpus via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-story178.json --run-root output/runs/story178-approved-intake-handoff`.
          All 10 cases wrote stamped `intake_handoff_v1` rows; the 6 maintained
          launch cases also stamped the expected first downstream artifact with
          bounded `--end-at` execution. Evidence inspected manually includes
          `output/runs/story178-approved-intake-handoff/tbotb-mini/intake_handoff.jsonl`
          plus
          `output/runs/story178-approved-intake-handoff-tbotb-mini-recipe-born-digital-pdf-marker-lite-html-mvp/01_extract_pdf_marker_lite_html_v1/pages_html.jsonl`,
          `output/runs/story178-approved-intake-handoff/onward-unknown/intake_handoff.jsonl`
          plus
          `output/runs/story178-approved-intake-handoff-onward-unknown-recipe-pdf-ocr-html-mvp/01_extract_pdf_images_fast_v1/pages_images_manifest.jsonl`,
          `output/runs/story178-approved-intake-handoff/rfp/intake_handoff.jsonl`
          plus
          `output/runs/story178-approved-intake-handoff-rfp-recipe-born-digital-pdf-non-toc-html-mvp/01_extract_pdf_marker_lite_html_v1/pages_html.jsonl`,
          and representative explicit non-launch rows such as
          `output/runs/story178-approved-intake-handoff/scanned-prose-mini/intake_handoff.jsonl`
          and `output/runs/story178-approved-intake-handoff/memorial-card/intake_handoff.jsonl`.

    attempts:
      - id: "001"
        date: 2026-04-01
        status: succeeded
        approach: "Story 176 driver-backed confirmed-handoff proofs on supported image-directory and PDF inputs"
        note: >
          Fresh runs via the maintained confirmed-handoff recipe launched the
          expected explicit recipe on the reviewed image-directory slice
          (`onward-to-the-unknown-images` -> `recipe-images-ocr-html-mvp.yaml`),
          the reviewed scanned-PDF slice (`Onward to the Unknown.pdf` ->
          `recipe-pdf-ocr-html-mvp.yaml`), and the reviewed book-like
          born-digital slice (`testdata/tbotb-mini.pdf` ->
          `recipe-born-digital-pdf-marker-lite-html-mvp.yaml`). Each run wrote
          a stamped `intake_handoff_v1` row with the resolved launch flag/path,
          downstream `run_id`, and `terminal_outcome = launched`, then produced
          the first expected downstream stamped artifact.
      - id: "002"
        date: 2026-04-02
        status: succeeded
        approach: "Story 178 corpus-wide approved-handoff harness across the locked 10-document maintained intake PDF corpus"
        note: >
          Fresh corpus-wide run via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-story178.json --run-root output/runs/story178-approved-intake-handoff`
          completed `10/10` cases with `pass_rate = 1.0`, `overall = 1.0`,
          `launched = 6`, `skipped = 4`, and `failed_runs = 0`. The 6
          maintained launch cases stamped `intake_handoff_v1` plus the expected
          first downstream artifact under `output/runs/`; the 4
          `no-recipe-needed` cases stamped explicit skipped handoff rows with
          `terminal_reason = no_recipe_needed`. Image-directory remains covered
          only by Story 176's representative proof and is still outside this
          locked corpus-wide surface.
      - id: "003"
        date: 2026-04-02
        status: succeeded
        approach: "Story 180 widened approved handoff in place via a dedicated approved-handoff corpus plus one repo-owned image-directory case"
        note: >
          Split `run_approved_intake_handoff_eval.py` onto
          `benchmarks/golden/approved-intake-handoff/corpus.json` so
          `auto-book-type-detection` could stay on its locked 10-case
          recommendation-only PDF corpus. Fresh rerun via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-story180.json --run-root output/runs/story180-approved-intake-handoff`
          completed `11/11` cases with `pass_rate = 1.0`, `overall = 1.0`,
          `launched = 7`, `skipped = 4`, and `failed_runs = 0`. The added
          repo-owned image-directory case launched
          `configs/recipes/recipe-images-ocr-html-mvp.yaml` with
          `launch_input_flag = --input-images` and stamped
          `01_images_dir_to_manifest_v1/pages_images_manifest.jsonl`; the prior
          launched and skipped PDF paths remained green on the same run.
      - id: "004"
        date: 2026-04-07
        status: succeeded
        approach: "Story 194 office-family handoff boundary probe on repo-owned DOCX/XLSX/PPTX fixtures"
        note: >
          Story 194 kept the official approved-handoff corpus unchanged at the
          maintained `pdf` / `images_dir` scope, then added the rerunnable
          probe corpus `benchmarks/input/office-intake-boundary-corpus.json`
          plus explicit scope blocking in the harness and a clearer
          direct-entry-office rejection reason in
          `modules/intake/intake_plan_utils.py`. Fresh rerun via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --corpus benchmarks/input/office-intake-boundary-corpus.json --output benchmarks/results/approved-intake-handoff-story194-office-boundary.json --run-root output/runs/story194-approved-intake-handoff-office-boundary`
          produced three inspectable blocked rows in
          `benchmarks/results/approved-intake-handoff-story194-office-boundary.json`
          with `failure_step = scope`: `docx` and `xlsx` report
          `scope_policy = direct_explicit_recipe_only`, and `pptx` reports
          `scope_policy = runtime_blocked`. Additional same-pass dry-run
          evidence from `prepare_confirmed_handoff(...)` now reports
          `direct_entry_recipe_outside_confirmed_handoff_scope:docx` and
          `direct_entry_recipe_outside_confirmed_handoff_scope:xlsx` for the
          maintained direct-entry office recipes instead of the older generic
          `unsupported_recommended_recipe:*` bucket.
      - id: "005"
        date: 2026-04-08
        status: succeeded
        approach: "Story 197 office-family handoff boundary probe rerun after the maintained PPTX direct-entry lane landed"
        note: >
          Story 197 added the maintained PPTX direct-entry recipe plus
          `DIRECT_ENTRY_ONLY_RECIPES` wiring, then reran
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --corpus benchmarks/input/office-intake-boundary-corpus.json --output benchmarks/results/approved-intake-handoff-story197-office-boundary.json --run-root output/runs/story197-approved-intake-handoff-office-boundary`.
          The fresh result file
          `benchmarks/results/approved-intake-handoff-story197-office-boundary.json`
          now records three inspectable blocked rows with `failure_step =
          scope`, and all three office kinds (`docx`, `xlsx`, `pptx`) report
          `scope_policy = direct_explicit_recipe_only`. Same-pass helper checks
          now also route the maintained PPTX recipe through
          `direct_entry_recipe_outside_confirmed_handoff_scope:pptx`, keeping
          the confirmed-handoff boundary aligned with the direct-entry runtime.
      - id: "006"
        date: 2026-04-10
        status: succeeded
        approach: "Current-head approved-handoff corpus refresh after Stories 200-205 widened direct-entry and driver input surfaces"
        score_before: 1.0
        score_after: 1.0
        note: >
          Fresh rerun via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-improve-eval-20260410.json --run-root output/runs/approved-intake-handoff-improve-eval-20260410`
          completed `11/11` maintained corpus cases with `pass_rate = 1.0`,
          `overall = 1.0`, `launched = 7`, `skipped = 4`, and `failed_runs =
          0` on `601353b`. The newer direct-entry families remained outside
          the automation surface by design: temporary boundary probing in
          `benchmarks/results/approved-intake-handoff-direct-entry-boundary-20260410.json`
          confirmed `web-page`, `epub`, `email-eml`, and `email-mbox` all
          return `failure_step = scope` with
          `scope_policy = direct_explicit_recipe_only`, so no golden or scorer
          change was needed.
      - id: "007"
        date: 2026-04-13
        status: succeeded
        approach: "Current-head approved-handoff corpus refresh after Story 218 added mixed-folder input plumbing and a new archive entry surface"
        score_before: 1.0
        score_after: 1.0
        note: >
          Classification: benchmark-harness-wrong / scope-metadata stale, not
          golden-wrong. A fresh pre-fix boundary probe on current `HEAD`
          showed the newly maintained `mixed-archive` and `mixed-folder` entry
          kinds still returned `scope_policy = unsupported_input_kind` even
          though Story 205 and Story 218 had already established them as direct
          explicit-recipe lanes. Fix: extend
          `benchmarks/scripts/intake_scope.py`, add focused coverage in
          `tests/test_approved_intake_handoff_benchmark.py` and
          `tests/test_auto_book_type_detection_benchmark.py`, and commit the
          reusable probe corpus
          `benchmarks/input/non-office-direct-entry-boundary-corpus.json`.
          Fresh rerun via
          `python benchmarks/scripts/run_approved_intake_handoff_eval.py --output benchmarks/results/approved-intake-handoff-improve-eval-20260413.json --run-root output/runs/approved-intake-handoff-improve-eval-20260413`
          then completed `11/11` maintained corpus cases with `pass_rate =
          1.0`, `overall = 1.0`, `launched = 7`, `skipped = 4`, and
          `failed_runs = 0` on `38c78a8`. Same-pass boundary probing via
          `benchmarks/results/approved-intake-handoff-direct-entry-boundary-20260413.json`
          and
          `benchmarks/results/auto-book-type-detection-direct-entry-boundary-20260413.json`
          confirmed `web-page`, `epub`, `email-eml`, `email-mbox`,
          `mixed-archive`, and `mixed-folder` now all report
          `scope_policy = direct_explicit_recipe_only`. Manual inspection in
          the same pass covered the launched PDF row
          `output/runs/approved-intake-handoff-improve-eval-20260413/tbotb-mini/intake_handoff.jsonl`
          plus its first downstream HTML artifact, the explicit skip row
          `output/runs/approved-intake-handoff-improve-eval-20260413/scanned-prose-mini/intake_handoff.jsonl`,
          and the image-directory launch row
          `output/runs/approved-intake-handoff-improve-eval-20260413/handwritten-notes-mini-images/intake_handoff.jsonl`
          plus its first downstream image manifest row.

    retry_when:
      - condition: workflow-change
        note: >
          Rerun when `run_dispatch_v1`, supported input families, or the driver
          input-override plumbing changes.

  - id: layout-section-splitting
    name: "C3 Proof: Maintained Layout Section Splitting"
    type: quality
    description: >
      Repo-owned 5-document benchmark for the bounded maintained C3 slice:
      3 DOCX fixtures plus 2 flat born-digital PDFs. Scores the accepted final
      `doc_web_bundle_manifest_v1` / `doc_web_provenance_block_v1` outputs
      against checked-in section-order, non-split HTML, and provenance
      assertions, and also records one thinner challenger per family to keep
      pressure on the current seam.
    story_refs: ["181"]
    category_refs: ["spec:3", "spec:6", "spec:8", "spec:9"]
    compromise_refs: ["C3"]
    spec_compromise: C3
    runner: manual
    command: "python benchmarks/scripts/run_layout_section_splitting_eval.py --output benchmarks/results/layout-section-splitting-story181.json --run-root output/runs/story181-layout-benchmark"
    scorer: benchmarks/scorers/layout_section_splitting.py
    golden: benchmarks/golden/layout-section-splitting/corpus.json

    target:
      metric: pass_rate
      value: 1.0
      note: >
        All 5 bounded maintained cases must clear the checked-in section-order,
        non-split HTML, and provenance assertions. Challenger rows are tracked
        as comparison evidence rather than part of the pass gate.

    scores:
      - model: "Story 181 maintained layout benchmark (quality + challenger comparison)"
        metrics:
          pass_rate: 1.0
          overall: 1.0
          docs: 5
          failed_runs: 0
          challenger_cases: 2
          challenger_real_competitors: 0
          challenger_overall: 0.6
        measured: 2026-04-02
        note: >
          Story 181 added
          `benchmarks/golden/layout-section-splitting/corpus.json`,
          `benchmarks/scorers/layout_section_splitting.py`, and
          `benchmarks/scripts/run_layout_section_splitting_eval.py`, then reran
          the bounded maintained corpus via
          `python benchmarks/scripts/run_layout_section_splitting_eval.py --output benchmarks/results/layout-section-splitting-story181.json --run-root output/runs/story181-layout-benchmark`.
          All 5 repo-owned cases passed on fresh `driver.py` outputs. Manual
          inspection in the same pass covered
          `output/runs/story181-layout-benchmark-docx-mini/output/html/manifest.json`,
          `output/runs/story181-layout-benchmark-docx-nested/output/html/chapter-001.html`,
          `output/runs/story181-layout-benchmark-flat-born-digital-mini/output/html/provenance/blocks.jsonl`,
          and
          `output/runs/story181-layout-benchmark-flat-born-digital-form-mini/output/html/chapter-001.html`.
          Classification: quality benchmark with challenger comparison. The two
          thinner representative challengers stayed non-competitive on the same
          bounded slice: the DOCX single-entry baseline derived from
          `01_unstructured_docx_intake_v1/elements.jsonl` misses required
          splits, and the flat extract-stage `doc_web_bundle` preserves
          page-level provenance but fails the checked-in section expectations.

    attempts:
      - id: "001"
        date: 2026-04-02
        status: succeeded
        approach: "Story 181 first maintained layout benchmark on the repo-owned DOCX + flat born-digital slice"
        note: >
          Fresh run via
          `python benchmarks/scripts/run_layout_section_splitting_eval.py --output benchmarks/results/layout-section-splitting-story181.json --run-root output/runs/story181-layout-benchmark`
          completed all 5 bounded corpus cases with `pass_rate = 1.0`,
          `overall = 1.0`, and `failed_runs = 0`. Representative challenge
          rows also ran in the same pass and remained non-competitive
          (`challenger_real_competitors = 0`, `challenger_overall = 0.6`).

    retry_when:
      - condition: new-subject-model
        note: >
          Rerun when a model-only or thinner challenger for DOCX or flat
          section ownership looks credible on the same bounded corpus.
      - condition: new-input-family
        note: >
          Expand or rerun when a new maintained section-bearing input family
          lands, or when the bounded corpus itself changes.

  - id: born-digital-pdf-quality-surface
    name: "C3 Proof: Maintained Born-Digital PDF Quality Surface"
    type: quality
    description: >
      Repo-backed 6-case benchmark for the bounded maintained born-digital PDF
      surface. The passing gate is the four repo-owned supported cases across
      both explicit lanes: two book-like fixtures (`tbotb-mini`,
      `born-digital-handbook-mini`) plus two flat/non-TOC fixtures
      (`flat-born-digital-mini`, `flat-born-digital-form-mini`). The two
      shared local PDFs (`rfp`, `release-forms`) rerun as optional
      comparison-only evidence on the same harness when they are available.
      The scorer checks stamped `page_html_v1` row counts, portion artifacts,
      final bundle ordering/titles, HTML snippets, and block provenance.
    spec_compromise: C3
    runner: manual
    command: "python benchmarks/scripts/run_born_digital_pdf_eval.py --output benchmarks/results/born-digital-pdf-story196.json --run-root output/runs/story196-born-digital-benchmark"
    scorer: benchmarks/scorers/born_digital_pdf.py
    golden: benchmarks/golden/born-digital-pdf/corpus.json
    story_refs: ["196"]
    category_refs: ["spec:1", "spec:3", "spec:6", "spec:7", "spec:8"]
    compromise_refs: ["C3"]

    target:
      metric: pass_rate
      value: 1.0
      note: >
        All 4 repo-owned supported cases must clear the checked-in born-digital
        benchmark. Optional comparison-only local cases are reported, not part
        of the passing gate.

    scores:
      - model: "Story 196 maintained born-digital benchmark"
        metrics:
          pass_rate: 1.0
          overall: 1.0
          docs: 6
          supported_docs: 4
          comparison_docs: 2
          comparison_pass_rate: 1.0
          failed_runs: 0
        measured: 2026-04-08
        note: >
          Story 196 added
          `benchmarks/golden/born-digital-pdf/corpus.json`,
          `benchmarks/scorers/born_digital_pdf.py`, and
          `benchmarks/scripts/run_born_digital_pdf_eval.py`, then reran the
          maintained born-digital corpus via
          `python benchmarks/scripts/run_born_digital_pdf_eval.py --output benchmarks/results/born-digital-pdf-story196.json --run-root output/runs/story196-born-digital-benchmark`.
          The result file
          `benchmarks/results/born-digital-pdf-story196.json` recorded
          `docs = 6`, `supported_docs = 4`, `comparison_docs = 2`,
          `pass_rate = 1.0`, `overall = 1.0`, `comparison_pass_rate = 1.0`,
          and `failed_runs = 0`. Manual inspection in the same pass covered
          `output/runs/story196-born-digital-benchmark-born-digital-handbook-mini/output/html/manifest.json`,
          `output/runs/story196-born-digital-benchmark-born-digital-handbook-mini/output/html/chapter-001.html`,
          `output/runs/story196-born-digital-benchmark-flat-born-digital-mini/output/html/provenance/blocks.jsonl`,
          `output/runs/story196-born-digital-benchmark-rfp/output/html/manifest.json`,
          and
          `output/runs/story196-born-digital-benchmark-release-forms/output/html/chapter-001.html`.

    attempts:
      - id: "001"
        date: 2026-04-08
        status: succeeded
        approach: "Story 196 first maintained born-digital benchmark on the widened repo-owned supported slice plus optional comparison-only local PDFs"
        note: >
          Fresh run via
          `python benchmarks/scripts/run_born_digital_pdf_eval.py --output benchmarks/results/born-digital-pdf-story196.json --run-root output/runs/story196-born-digital-benchmark`
          completed all 6 present cases with `pass_rate = 1.0`,
          `comparison_pass_rate = 1.0`, `overall = 1.0`, and
          `failed_runs = 0`. The bounded passing gate is the 4 repo-owned
          supported cases; the 2 shared local comparison-only PDFs also reran
          cleanly on the same harness in this environment.

    retry_when:
      - condition: workflow-change
        note: >
          Rerun when either maintained born-digital recipe, the Marker-lite
          extract stage, or the bundle/provenance build path changes.
      - condition: corpus-change
        note: >
          Rerun when the supported repo-owned born-digital corpus changes, when
          new comparison-only PDFs are added, or when a currently optional local
          comparison case becomes repo-owned enough to join the passing gate.

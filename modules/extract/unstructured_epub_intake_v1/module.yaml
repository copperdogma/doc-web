module_id: unstructured_epub_intake_v1
stage: extract
entrypoint: modules/extract/unstructured_epub_intake_v1/main.py
input_schema: null
output_schema: unstructured_element_v1
default_params:
  save_raw: false
param_schema:
  properties:
    save_raw:
      type: boolean
      description: "Save raw Unstructured EPUB output for debugging"
notes: "Partition EPUB with Unstructured into elements.jsonl while preserving pageless EPUB-native anchors and package metadata."

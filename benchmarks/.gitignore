# Large data files — regenerate by running promptfoo eval
results/*.json
results/visual-comparison/

# Maintained crop benchmark fixtures live under input/source-pages-b64/ and
# input/crop-validation-b64/. Keep any ad hoc raw source-page copies ignored.
input/source-pages/

# Golden crop images (large binaries)
golden/crops/
